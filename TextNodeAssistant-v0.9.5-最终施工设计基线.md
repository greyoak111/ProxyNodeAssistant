# TextNodeAssistant v0.9.5 最终施工设计基线

> 文档性质：产品边界、技术架构、施工顺序、迁移策略和验收标准的统一基线  
> 文档状态：设计冻结并进入实现/全链路验收；代码施工已获授权，真实 VPS 变更必须先备份并按验收矩阵执行  
> 对外版本：始终显示 `v0.9.5`  
> 适用端：Windows 桌面客户端、Android 客户端、Linux VPS 内嵌工具包  
> 隐私要求：源码、EXE、APK、压缩包、文档和测试夹具不得包含任何真实 VPS IP、域名、邮箱、账号、密码、Token 或密钥

---

## 0. 使用本文件的方法

本文件是后续施工的约束来源，不是愿望清单。实现阶段必须遵守以下规则：

1. 用户已经确认的产品结构不得由实现者自行简化、替换或降级。
2. 每个新增远端施工动作必须同时设计检测、幂等、验证、回滚和拆除动作。
3. 没有真实实现的功能不得出现在界面中，不保留灰色按钮或“以后再做”的入口。
4. 操作成功必须以真实探测结果为准，不能以脚本打印了成功文字为准。
5. 任何失败分支不得继续复制空交接单、猜测端口、打开错误页面或执行后续施工。
6. 本文件与旧实现冲突时，以本文件的“已冻结决策”为目标；旧实现仅作为迁移来源。
7. 后续若用户改变设计，必须先更新本文件，再修改代码。

### 0.1 文档中的约束词

- **必须**：发布前不可缺失。
- **禁止**：不得通过配置或隐藏入口绕过。
- **默认**：用户没有显式改变设置时采用的行为。
- **建议**：可以在不破坏硬约束的前提下调整实现细节。
- **待验证**：设计已确定，但仍需通过静态测试、桌面测试或真实 VPS 验收。

---

## 1. 已冻结的产品决策

以下内容已经拍板，后续不得擅自改写。

### 1.1 版本规则

- 对外产品版本固定为 `v0.9.5`。
- Windows、Android、Linux 工具包、说明书、窗口标题和发布产物都显示 `v0.9.5`。
- 后续变化仅通过内部字段识别：

```text
TOOLKIT_BUILD_REVISION
STATE_SCHEMA_VERSION
DRIVE_SCHEMA_VERSION
DEVICE_ADMISSION_SCHEMA_VERSION
DESKTOP_BUILD_ID
ANDROID_BUILD_ID
```

- 远端构建比本地新时，客户端必须停止降级并建议换用更新构建的 v0.9.5 客户端。

### 1.1.1 正式名称与旧名称迁移

正式产品名称固定为：

```text
TextNodeAssistant
简称：TNA
控制台标识：TNA // NODE OPS
```

以下全部必须使用新名称：

- Windows EXE、安装包、快捷方式、窗口标题、进程描述和软件图标元数据。
- Android 应用名称、APK 文件名、应用内标题和通知渠道。
- GitHub 仓库、发布页、源码包、说明书和用户可见日志前缀。
- 新安装的远端服务、工具包目录、配置目录、状态目录和维护命令。
- Windows Credential Manager 项、恢复目录和新生成的 `.tna` 恢复包。

“代理”“VLESS”“XHTTP”等词只在高级控制台的必要技术说明中按真实功能使用，不得再把 `Proxy` 作为产品品牌、可执行文件名、窗口主标题或公开包名的一部分。

旧构建升级兼容规则：

- 升级器必须只读识别旧产品名、旧简称、旧日志/状态前缀、旧服务名、旧远端目录和旧 `.pna` 恢复包。
- 迁移必须先备份、再写入 TNA 新路径、验证服务和身份均正常后，才删除或停用旧路径；失败时回滚到原可用状态。
- 状态枚举中旧的 `*_BY_PNA` / `*_AFTER_PNA` 必须映射到新的 `*_BY_TNA` / `*_AFTER_TNA`，资源归属不得因改名丢失。
- 新构建保持读取旧格式的能力，但只写新格式；新安装绝不再创建旧品牌路径或前缀。
- “拆除施工和恢复基线”必须同时识别迁移前后的资源，避免改名后旧服务变成无法拆除的孤儿。
- 对外界面默认不展示旧品牌；只有迁移失败的高级诊断报告可以标明具体旧资源名称。

改名不改变公开版本号，所有平台仍显示 `v0.9.5`。

### 1.2 双层客户端结构

客户端必须是两层完整产品，不得把现有高级控制台降级成后备命令行执行器。

```text
启动 TNA
  └─ 普通账号登录
       └─ 私人网盘前台（日常工作区）
            ├─ 文件浏览
            ├─ 上传 / 下载
            ├─ 传输任务
            ├─ 节点切换
            ├─ 挂载 / 卸载
            └─ 高级运维控制台
                  └─ 本地 admin 门禁
                       └─ 当前完整 TNA 高级图形界面
```

- **外层**：以私人网盘客户端形态呈现，同时接受普通网盘账号和本机 `admin` 账号密码登录。
- **内层**：完整保留现有 TNA 图形运维控制台和全部有效功能。
- **禁止**：把内层改成纯日志窗口、隐藏命令行、缩水设置页或仅供故障时使用的后备入口。
- 远端脚本输出、结构化提示和施工日志继续显示在内层各操作的工作流页面中。

外层登录的两种身份必须明确显示：

```text
普通账号登录：进入被授权的私人网盘空间
admin 登录：进入管理员自己的私人网盘空间，并可进一步打开高级运维控制台
```

`admin` 登录外层不代表把本机 admin 密码发送给 copyparty。客户端在本机验证 admin 后，使用当前受信设备的远端访问能力进入共享 `ADMIN_SPACE_ID`；本机 admin 密码始终只留在本机。

初始登录界面必须直接提供“注册普通网盘账号”，不能要求用户先登录或进入其他页面才能找到注册入口。注册请求必须从已经绑定或批准的受信设备经 SSH 隧道发出，不需要进入高级控制台或输入 admin 密码。注册成功得到的是节点/空间级普通账号，不绑定发起注册的那台设备；同一账密可以在该节点的所有受信设备上进入同一空间。每台 VPS 最多创建 2 个普通网盘账号，加上固定的 `admin` 空间后最多形成 3 个相互隔离的文件空间。

### 1.3 admin 门禁

- 首次启动必须创建本机固定管理员名 `admin` 的密码，并确认两次。
- 从外层进入高级运维控制台时，默认必须验证本机 `admin` 密码。
- 内层设置提供：

```text
进入高级功能时验证 admin 密码：开启 / 关闭
```

- 默认开启。
- 设置只属于当前设备，不同步到 VPS 或其他设备。
- 修改该开关前必须再次验证当前 `admin` 密码。
- 关闭入口门禁后，高风险操作仍必须重新验证 admin，不能因入口免密而暴露全部秘密或拆除能力。
- 建议提供高级会话锁定策略：立即、5 分钟、15 分钟、本次运行期间。
- 本机 admin 凭据必须可恢复，禁止只保存不可逆校验值后让用户遗失密码时无路可走。
- 首次创建或修改 admin 密码时，必须同时更新系统凭据库记录和可导出的加密恢复包，并把准确保存位置展示给用户。
- 本机 admin 的主动改密入口只能位于内层高级控制台；外层网盘设置不得提供 admin 改密或重置按钮。

### 1.4 网盘固定为无域名模式

- 网盘不申请、不要求、不保存任何专用域名或邮箱。
- 网盘不使用 Cloudflare、CDN、ACME、源站证书或 Origin Rule。
- 网盘不使用 Nginx 公网回源。
- 网盘服务仅绑定 VPS 的 `127.0.0.1`。
- Windows 和 Android 只通过 SSH 本地端口转发访问网盘。
- 网盘设计与代理线路的灰云、橙云、双路状态完全解耦。
- 代理线路仍按其自身模式要求用户填写对应域名和邮箱；“网盘无域名”不能被错误理解为代理线路也不需要域名。
- 网盘不再是可选组件，而是所有 TNA 受管代理节点必须具备的基础组成部分，也是正常流量特征伪装的一部分。
- 菜单 `[1]` 只要选择安装、升级或收敛任一代理模式，就必须同时安装、升级并验收网盘；界面不得提供“跳过网盘”复选框。
- 代理施工成功但网盘未就绪时，整个 `[1]` 工作流必须判定为未完成，不能交付一个“代理已完成、网盘以后再装”的半成品节点。

### 1.5 唯一安装入口

- 高级控制台菜单 `[1]` 继续是唯一允许上传、安装、升级、bootstrap 或收敛远端内嵌工具包的入口。
- `[1]` 同时是拆除后重新施工和受管组件缺失后的唯一恢复入口。它必须识别哪些组件已拆除、哪些仍被保留、上次拆除是否完整提交，并据此只恢复缺失部分或执行全新施工。
- 其他功能可以执行其明确职责内的维护动作，例如轮换密码、设备批准、备份、恢复和拆除，但不得暗中上传或重装工具包。
- 工具包缺失、过旧或不完整时，维护功能必须要求先运行 `[1]`；`[1]` 自身必须能够依据施工台账、拆除报告和现场证据修复，不得再把用户踢到另一个不存在的“有限卸载/修复”入口。
- “拆除施工和恢复基线”是明确的删除入口，不属于安装入口，也禁止在拆除中触发 bootstrap。

### 1.6 “拆除施工和恢复基线”必须长期保留

- 产品必须“能装、能查、能升级、能回滚、能拆”。
- “拆除施工和恢复基线”不能被合并到普通卸载按钮或隐藏菜单。
- 完整拆除必须保留 SSH 恢复路径，最后才撤销当前 controller。
- 每个新增施工动作必须同步登记其拆除办法，否则该功能不得进入正式构建。
- 代理仍存在时，“拆除施工和恢复基线”只允许“仅拆代理、保留强制网盘”或“整体拆除并恢复基线”。此时禁止显示单独停用、卸载或删除网盘的选项。
- 只有检测到代理已经完整拆除、节点处于“仅剩网盘”的受管状态时，才允许显示“拆除剩余网盘并恢复基线”。
- “整体拆除”不是保留 TNA 文件的软卸载，而是撤销代理、网盘及全部 TNA 施工并恢复有证据记录的原始基线。

### 1.7 永久取消的功能

以下项目直接取消，不进入本版或后续 v0.9.5 构建，也不留占位：

- 单盘符跨 VPS 无缝抢占。
- 复杂证书漂移图表。
- 公开分享中心。
- 高级文件预览器。
- 跨 VPS 合并为一个虚拟网盘。

保留的多 VPS 能力是：节点相互独立、任务绑定节点、分别挂载盘符、用户显式切换当前节点。

---

## 2. 当前实现与目标实现的边界

### 2.1 当前可复用部分

当前仓库已经具备以下基础，应迁移和强化而不是重写后丢失：

- Windows WPF 全图形高级控制台。
- Go 工作流核心、OpenSSH 调用、AskPass 和图形提示代理。
- 临时密码与节点长期 key 双模式。
- 最近 VPS 历史、key 备份和恢复。
- 菜单 `[1]` 的工具包版本检测和幂等分支。
- 3x-ui 面板回环隧道。
- 结构化诊断、修复、凭据、备份、流量和拆除功能。
- 现有设备准入 v1 和每设备 VLESS 基础。
- 当前 copyparty 回环服务、账号轮换和 CRUD 验收基础。
- Android Compose 客户端、加密仓库、SSH 引擎和隧道服务。

### 2.2 当前需要替换或升级的部分

- 现有 GUI 的主要工作流仍由通用文本提示驱动；外层网盘必须改为真实文件客户端界面。
- 当前 copyparty 固定端口必须改为有状态的回环端口分配与冲突检测。
- 当前固定 2/3 GiB 配额必须改为可回滚的自适应配额。
- 当前网盘的 Nginx/Cloudflare 候选路径必须移除，不再显示。
- 当前设备邀请在实际 SSH/凭据领取成功前就可能被消费，必须升级为可恢复状态机。
- 当前设备签名身份、SSH 登录身份、VLESS 身份和访问网盘注册/登录端点的设备能力必须合并为同一次设备准入交付；普通网盘账号本身是节点/空间级账号，不在准入时绑定到设备。
- 施工台账必须覆盖新增网盘和身份资源，旧节点进入谨慎接管模式。
- 当前内层高级控制台必须完整保留并嵌入新的双层产品结构。

---

## 3. 用户、身份与权限模型

系统中存在四类不同身份，禁止混用密码或权限。

### 3.1 本机 admin

用途：

- 直接使用 `admin` 用户名和本机 admin 密码登录外层私人网盘前台。
- 访问当前设备被授权的管理员网盘空间。
- 打开高级运维控制台。
- 解锁本机秘密仓库。
- 显示或复制完整交接单。
- 进行设备批准、密码轮换和拆除。

特点：

- 用户名固定为 `admin`。
- 每台设备独立设置，可以不同。
- 不发送到 VPS。
- 不作为 SSH、3x-ui 或 copyparty 登录密码。
- admin 登录外层后，远端文件访问使用当前受信设备的远端访问能力进入共享管理员空间；本机 admin 密码不能与远端能力合并、复用或同步。
- 当前设备尚未绑定节点或没有可用远端访问能力时，admin 仍可进入本地客户端外壳，但文件区必须明确提示先绑定、恢复或批准设备，不能伪造一个空网盘。
- admin 的真实用户名和密码必须存在于当前操作系统保护的可恢复凭据记录中；日常验证仍使用独立的加盐校验器，不能因为可恢复而在普通配置或日志中保存明文。
- 用户可以通过“本机 admin 恢复”流程，在验证 Windows/Android 当前系统身份或提供恢复包与恢复码后恢复原密码和本机凭据记录。
- 恢复流程不允许在外层直接设置一个新 admin 密码。需要主动改密时，必须先恢复并登录，再进入内层高级控制台操作。

### 3.2 普通网盘账号

用途：在外层注册并登录私人网盘前台。

允许：

- 浏览授权空间。
- 上传、下载、新建目录、重命名和删除。
- 查看自己的传输任务。
- 挂载自己的空间。

禁止：

- 安装或升级 VPS。
- 修改代理拓扑。
- 查看 VPS、SSH、3x-ui 或 Cloudflare 秘密。
- 批准或吊销其他设备。
- 打开“拆除施工和恢复基线”。
- 获得 copyparty 管理权限。

普通网盘账号是节点/空间级账号，不是设备账号：

- 普通网盘账号可以由任一当前受信设备在外层自助注册。
- 注册请求必须经过该设备已经建立的 SSH 隧道，并证明设备状态为 active。
- 注册成功后生成稳定 `DRIVE_ACCOUNT_ID` 和 `DRIVE_SPACE_ID`。
- 同一个普通账号在该节点的所有受信设备上使用相同用户名和密码，访问同一个 `DRIVE_SPACE_ID`。
- 新受信设备不自动创建另一个普通账号；用户可以直接输入已有普通账密进入原空间，也可以注册新的普通账号和空间。
- 每台 VPS 最多存在 2 个普通网盘账号；固定 `admin` 身份不计入普通账号数量，但其 `ADMIN_SPACE_ID` 计入文件空间总数。因此单台 VPS 最多有 3 个空间：1 个 admin 空间和 2 个普通账号空间。
- 达到上限后，登录页仍显示注册入口，但必须标记“本节点普通账号已满（2/2）”；点击后只允许返回登录、登录已有账号，或由 admin 进入内层账号管理停用/删除旧账号，不能覆盖现有账号或偷偷创建第三个账号。
- 吊销设备只撤销那台设备的 SSH、VLESS 和远端访问能力，不删除普通账号，也不影响其他受信设备继续使用同一账号。
- 禁用或删除普通账号会影响所有使用该账号的受信设备，这是账号级动作，不是设备级动作。
- 注册资格来自 active 设备身份，不来自公网匿名请求；网盘仍然没有公网注册入口。
- 本机 `admin` 是保留用户名，不能通过普通注册流程创建、覆盖或抢占。

普通账号与 admin 在外层的区别：

| 能力 | 普通账号 | admin |
|---|---:|---:|
| 登录外层网盘前台 | 是 | 是 |
| 访问自身授权文件 | 是 | 是 |
| 进入高级运维门禁 | 可以发起，但必须提供 admin 密码 | 可以发起，门禁开启时仍需重新验证 |
| 管理节点、设备和凭据 | 否 | 通过高级控制台执行 |
| 拆除施工和恢复基线 | 否 | 通过高风险二次认证后执行 |
| 修改自己的登录密码 | 可在外层输入旧密码并两次确认新密码 | 只能进入内层高级控制台修改 |

### 3.3 VPS SSH 身份

用途：建立管理连接和网盘隧道。

- 临时密码只交给 OpenSSH，不进入参数、日志或普通配置文件。
- 长期 key 按稳定节点身份和 SSH 用户分别管理。
- 新设备批准后获得独立 SSH key。
- 严格模式可以关闭公网 SSH 密码认证，但不能锁死 root 的本地控制台密码。
- 厂商 VNC、串口或救援控制台必须继续作为兜底。

### 3.4 面板与代理设备身份

- 3x-ui 账号与本机 admin、网盘账号完全分离。
- 每个批准设备获得独立 VLESS，支持暂停、恢复和吊销。
- 最后一个 active controller 受保护，不能直接吊销。

---

## 4. 本地凭据仓库与交接单

### 4.1 Windows

- 使用 Windows Credential Manager / DPAPI 保存秘密。
- 普通 JSON 只保存非秘密元数据和 Credential Manager 引用。
- SSH 私钥继续保存在按节点隔离的本机目录，并使用 Windows ACL。
- 不把密码、Token 或私钥放进命令行参数。

本机 admin 必须建立两层记录：

1. **日常验证记录**：加盐、内存困难 KDF 产生的校验值，用于普通登录和重新认证。
2. **可恢复凭据记录**：在 Windows Credential Manager 中保存可恢复的真实 `admin` 用户名和密码，目标名包含产品版本和本机 `DEVICE_ID`，由当前 Windows 用户的 DPAPI 边界保护。

建议目标命名：

```text
TextNodeAssistant/v0.9.5/local-admin/<DEVICE_ID>
```

除系统凭据库外，首次创建或修改 admin 密码时还必须生成独立加密恢复包。默认保存到用户明确可找到的位置，例如：

```text
%USERPROFILE%\Documents\TextNodeAssistant Recovery\<DEVICE_ID>\local-admin-recovery.tna
```

程序必须：

- 显示完整绝对路径。
- 提供“打开恢复包目录”。
- 生成一次性恢复码并完整显示、复制。
- 明确要求用户把恢复包与恢复码分开保存到密码管理器、加密 U 盘或其他可信位置。
- 允许用户另存恢复包。
- 密码修改后使旧恢复包失效并生成新包。

恢复包只保存经认证加密的 admin 恢复材料、设备标识、KDF 参数、创建时间和完整性校验，不保存 VPS、SSH、面板、Cloudflare 或网盘秘密。

恢复路径：

```text
同一 Windows 用户配置仍可用
→ 通过 Windows 系统身份确认
→ 读取 Credential Manager 可恢复记录
→ 恢复并显示原密码

系统重装或迁移到新设备
→ 导入 local-admin-recovery.tna
→ 输入恢复码
→ 校验恢复包
→ 恢复原 admin 凭据
→ 使用恢复出的原密码登录
→ 如需改密，进入内层高级控制台操作
```

没有系统凭据记录、恢复包和恢复码时，程序不能声称仍可恢复原密码，也不能在外层绕过内层门禁重设 admin。

### 4.2 Android

- 使用 Android Keystore 支持的加密仓库。
- 禁止将秘密写入普通 SharedPreferences、日志或可导出的明文文件。
- 截图、最近任务预览和通知不得显示完整秘密。
- Android 同样保存可恢复的 admin 凭据密文，并通过系统文件选择器导出 `.tna` 加密恢复包。
- 恢复包与恢复码分开保存；更改密码后旧恢复包失效。

### 4.3 普通网盘登录的保存策略

外层登录允许用户选择：

```text
仅本次使用
安全保存在当前设备
退出账号时清除
```

保存普通网盘凭据不等于保存 VPS 密码。

### 4.3.1 普通网盘账号外层改密

普通网盘用户可以在外层“账号与安全”页面修改自己的密码，不需要进入高级控制台，但必须完整提供：

```text
当前普通网盘密码
新密码
再次输入新密码
```

规则：

- 必须先用旧密码向远端网盘服务进行真实认证。
- 新密码两次输入必须逐字节一致。
- 不自动修剪首尾空格，不进行大小写转换或 Unicode 归一化。
- 新密码必须满足产品的强度和长度策略。
- 禁止仅凭当前已登录 Cookie 修改密码。
- 普通账号只能修改自身密码，不能修改其他账号。
- 用户名为 `admin` 时，外层不得显示这套改密表单，必须提示前往内层高级控制台。
- 普通账号是节点级身份，因此改密对所有受信设备立即生效；以后所有设备都必须使用新密码进入同一 `DRIVE_SPACE_ID`。

改密事务：

```text
验证旧密码
→ 备份当前账号哈希和 root-only 加密托管记录
→ 写入新密码哈希和新加密托管记录
→ 使用无 Cookie 新会话真实登录
→ 执行最小读写验证
→ 更新本地安全仓库
→ 更新完整交接单中的 DRIVE_PASSWORD
→ 使旧密码和旧会话失效
→ 成功提交
```

任一步失败都必须恢复旧哈希、旧托管记录和旧本地凭据。失败时不能把未经验证的新密码写进交接单，也不能使用户同时失去新旧密码。

成功后应完整显示新的普通网盘用户名和密码，并可经用户确认复制更新后的交接单。复制后提示清空剪贴板。

当前执行改密的设备更新自己的本地安全仓库。其他受信设备不得通过隐蔽同步自动取得新密码；它们下次认证失败时应明确提示账号密码已变化，由用户输入新密码或粘贴更新后的交接单。

### 4.4 完整交接单

交接单在 admin 重新验证后展示，必须包含真实且可验证的信息：

```text
NODE_ID
节点显示名称
VPS 主机或当前 endpoint
SSH 用户名
SSH 端口
SSH 登录方式
长期 key 指纹和本机路径
VPS 登录密码状态
面板用户名
面板真实密码或不可恢复状态
面板端口和 WebBasePath
面板本地隧道地址
DRIVE_ACCOUNT_ID
DRIVE_SPACE_ID
DRIVE_USERNAME
DRIVE_PASSWORD
当前代理模式
设备专属订阅
```

当前选择的普通网盘账号的真实用户名和密码必须直接进入完整交接单，不能只写“已创建”、星号或凭据引用。普通网盘凭据的注册、生成或轮换只有在同时满足以下条件后才能提交：

1. 新用户名和密码真实登录成功。
2. CRUD 验收通过。
3. 本地安全仓库已保存新凭据。
4. root-only 远端账号凭据托管密文已原子更新，权限为仅 root 可读，且 VPS 本身没有独立解密能力。
5. 完整交接单可以重新读取并逐字段验证出相同用户名和密码。

远端 copyparty 运行配置只保存密码哈希；用于受控交接的真实普通账号凭据只能以经认证加密的密文保存在独立 root-only 托管记录中。解密密钥由受信 controller 的设备加密公钥分别封装，不能与密文以可直接解密的形式共同留在 VPS。读取交接单必须在本机 admin 重新认证后，由当前受信 controller 在本机解密；服务器 root 单独取得密文不能还原普通账号密码。设备吊销必须删除该设备对应的密钥封装，但不删除账号密文；账号改密、禁用或删除时同步更新或删除账号级密文。所有 controller 都丢失且没有独立凭据备份时，程序必须如实显示不可恢复并引导显式轮换，不能伪造原密码。

本机 admin 密码不进入远端节点交接单。它使用前述系统凭据库和独立加密恢复包恢复，避免把本机控制权托管到 VPS。

既有密码只有哈希且无法恢复时，必须显示：

```text
UNKNOWN_NOT_RECOVERABLE
```

不能用星号、空字符串或推测值冒充真实密码。需要完整交接时，应显式询问是否轮换为新的可验证凭据。

对于普通网盘账号，发布目标要求交接单始终完整；如果旧账号只剩密码哈希，必须停止交付并引导 admin 显式轮换，轮换和真实登录验证成功后再生成交接单。

---

## 5. 外层私人网盘前台

### 5.1 启动页

提供：

```text
账号：普通网盘账号或 admin
密码：对应账号密码
登录
注册普通网盘账号
忘记 admin 密码 / 恢复本机 admin
添加或绑定节点
响应设备邀请
语言切换
关于与隐私说明
```

登录判定规则：

1. 用户名为精确的 `admin` 时，只使用本机 admin 验证器校验，不把该密码提交给远端文件服务。
2. admin 验证成功后，客户端使用当前受信设备的 SSH/远端访问能力建立隧道，进入共享管理员网盘空间。
3. 用户名不是 `admin` 时，通过 SSH 隧道向远端网盘服务验证普通账号和密码。
4. 两种身份都进入同一套外层文件客户端；界面根据身份显示权限和高级入口状态。
5. 认证失败必须明确区分“本机 admin 密码错误”“普通网盘账号错误”“设备未绑定”“SSH 隧道失败”，不能统一显示成含糊的登录失败。
6. “忘记 admin 密码”进入独立恢复页，支持当前系统用户恢复和导入 `.tna` 恢复包两条路径，不要求先进入高级控制台。

已经绑定的设备可以先使用设备 SSH key 建立隧道，再用普通网盘账号和密码验证文件空间。

尚未绑定的设备不能伪装为普通登录失败；界面必须进入明确的 `NO_BOUND_NODE` 状态。

### 5.1.1 新机器与未挂载节点

如果当前电脑从未登录、绑定或批准过任何 VPS，外层必须如实显示：

```text
节点状态：未挂载节点
网盘状态：尚不可用
原因：当前设备没有已绑定或已批准的 VPS

[进入高级运维控制台并添加 VPS]
[响应已有 controller 的设备邀请]
```

规则：

- 不显示空文件夹来冒充一个空网盘。
- 不显示已用 0 B、总容量 0 B 等伪造容量。
- 不尝试猜测历史 IP、默认 root、默认端口或其他设备的 key。
- 不把“没有节点”显示成普通账号密码错误。
- 主要引导是进入当前完整高级运维控制台，选择或新增 VPS，并通过 `[1]` 完成强制网盘在内的完整施工。
- 如果这台电脑是要加入已有节点的新设备，也可以选择响应设备邀请；绑定完成并领取网盘凭据后再刷新外层。
- 即使没有节点，高级控制台仍必须可以本地打开；admin 门禁规则照常生效。
- 新机器完成首次节点施工前，文件列表、传输、挂载和容量操作保持不可用，但高级控制台、admin 恢复和设备邀请入口必须可用。
- “注册普通网盘账号”按钮可以显示，但在 `NO_BOUND_NODE` 状态点击后只能解释必须先绑定节点，并提供进入高级控制台/响应邀请的入口，不能创建本地假账号。
- 注册入口的“显示”不等于“允许注册”。只有远端 `[1]` 完整施工已经提交并通过最终验收后，按钮才进入可提交状态；未施工、半施工、升级中、回滚中或施工失败时只能显示准确状态和“完成 VPS 施工”入口。

### 5.1.2 外层普通网盘账号注册

入口位置：应用启动后的初始登录界面，与“普通账号登录”“admin 登录”“响应设备邀请”并列显示。入口不得藏在登录后的个人中心或高级控制台中。

前置条件：

```text
当前节点已绑定
当前设备状态为 active trusted device
SSH 隧道已建立
远端节点处于 MANAGED_GRAY_WITH_DRIVE / MANAGED_ORANGE_WITH_DRIVE / MANAGED_DUAL_WITH_DRIVE 之一
最近一次 [1] 完整施工事务已经 COMMITTED
远端工具包、账号架构和网盘数据架构版本与当前客户端兼容
远端强制网盘健康
ADMIN_SPACE_ID、账号注册表、凭据托管和配额后端已经真实初始化
远端存储可写且容量安全检查通过
注册端点只在 VPS 回环地址可达
```

注册表单：

```text
普通网盘用户名
密码
再次输入密码
```

注册规则：

- 不要求输入本机 admin 密码，也不进入高级控制台。
- 客户端不得仅凭本地历史、旧交接单或“端口可连接”判定可以注册。注册端点必须从远端施工台账和实时健康检查共同生成结构化证明 `DRIVE_REGISTRATION_READY=1`。
- `PROXY_REMOVED_DRIVE_RETAINED` 只允许已有账号继续登录、读取和导出，不允许新注册普通账号；需要新增账号时必须先通过 `[1]` 恢复到三种完整受管状态之一。
- 远端处于 `BASELINE_UNMANAGED`、任何 `*_WITHOUT_DRIVE`、施工锁定、失败待恢复或版本不兼容状态时，服务端必须拒绝注册。客户端本地按钮、缓存或调试参数不得绕过。
- 服务端必须在预留用户名之前原子检查当前节点普通账号数；已有 2 个普通账号时返回结构化状态 `DRIVE_ACCOUNT_LIMIT_REACHED current=2 limit=2`，不得创建空间、哈希、托管记录或任何临时账号。
- 用户名必须通过长度、保留字和字符集校验；`admin`、`root` 及系统保留名不可注册。
- 用户名在当前节点内唯一，但账号不绑定当前 `DEVICE_ID`。
- 密码两次输入必须逐字节一致，不自动修剪、转换大小写或做 Unicode 归一化。
- 注册请求必须带当前 active 设备的签名证明，经 SSH 回环隧道送达；只拿到普通账号密码但设备未受信时，仍无法接近网盘服务。
- 不使用 CAPTCHA，也不开公网注册端点，因为外层注册只存在于受信设备的 SSH 隧道内。

成功事务：

```text
验证当前设备 active
→ 取得远端注册事务锁
→ 复核完整受管状态、最近施工 COMMITTED 和 DRIVE_REGISTRATION_READY=1
→ 复核网盘、账号架构、ADMIN_SPACE_ID、托管、配额和存储均可用
→ 锁定当前节点账号注册事务并复查普通账号数小于 2
→ 预留唯一用户名
→ 创建 DRIVE_ACCOUNT_ID
→ 创建稳定 DRIVE_SPACE_ID
→ 写入密码哈希
→ 写入 root-only 账号凭据托管密文，并封装给当前受信 controller
→ 使用无 Cookie 新会话真实登录
→ 完成最小 CRUD 验收
→ 保存到当前设备安全仓库（按用户选择）
→ 生成含真实普通账密的交接单
→ 自动进入该普通账号的外层空间
→ 提交注册
```

任一步失败必须删除未完成账号、空间占位、密码哈希和托管记录；用户名预留也要释放。客户端在远端成功提交前不得先创建本地账号档案、保存密码、生成交接单或显示注册成功。不能留下“本地有账号、VPS 没账号”或“VPS 有半个账号、永远登录不了”的幽灵账号。

跨设备使用：

1. 在设备 A 注册普通账号。
2. 设备 B 先通过 controller 邀请成为同一节点的受信设备。
3. 设备 B 建立自己的 SSH 隧道。
4. 在设备 B 外层输入设备 A 注册的同一普通用户名和密码。
5. 服务端返回同一个 `DRIVE_SPACE_ID`，两台设备看到同一空间和文件。

普通账号密码可以分别选择是否保存在每台设备的本地安全仓库；服务器上的账号和空间仍只有一份。

账号计数只统计普通账号，不按受信设备数量计算。同一个普通账号在十台受信设备登录仍只占一个名额和一个空间。账号删除必须先完成文件导出、空间迁移或明确的永久删除确认；仅停用账号仍占用名额，防止通过反复停用绕过两账号上限。

### 5.2 网盘首页

必须显示：

- 当前登录身份（普通账号或 admin）。
- 当前节点名称和稳定 `NODE_ID` 的短标识。
- SSH 隧道状态。
- 空间已用、上限和剩余。
- 最近上传/下载。
- 挂载状态。
- 明确的“高级运维控制台”入口。

### 5.3 文件列表

最小功能：

- 目录导航和面包屑。
- 名称、类型、大小、修改时间。
- 上传、下载、新建目录、重命名、移动、删除。
- 多选和批量操作。
- 排序和基础搜索。
- 操作失败的明确原因和重试。

本版不做高级预览器。文件可以交给系统默认程序打开，但不得在客户端内实现复杂媒体、Office 或代码预览系统。

### 5.4 传输队列

每个任务必须持久记录：

```text
TASK_ID
NODE_ID
DEVICE_ID
方向
本地路径
远端路径
文件大小
已完成字节
校验信息
状态
重试次数
创建时间
最后更新时间
```

任务状态至少包括：

```text
QUEUED
CONNECTING
RUNNING
PAUSED
RETRY_WAIT
COMPLETED
FAILED
CANCELLED
```

节点切换不能篡改已有任务的 `NODE_ID`。应用重启后应恢复可继续的任务。

### 5.4.1 切换网盘节点时的重新认证

用户在外层选择另一台 VPS 作为当前浏览或新挂载目标时，必须立即退出当前网盘账号的外层会话并返回初始登录界面。登录界面必须醒目显示即将切换到的节点名称、`NODE_ID` 短标识和已验证 SSH Host 指纹，随后要求用户使用该目标 VPS 上真实存在的普通网盘账号密码或本机 `admin` 身份重新登录。

切换事务：

```text
停止向旧 NODE_ID 创建新文件任务
→ 将旧节点仍在运行的传输任务保持绑定原 NODE_ID
→ 清除当前 UI 的账号、DRIVE_ACCOUNT_ID、DRIVE_SPACE_ID、Cookie、Token 和路径上下文
→ 选择并验证目标 NODE_ID 与 SSH Host 指纹
→ 返回带目标节点标识的初始登录界面
→ 用户对目标节点显式提交登录
→ 服务端返回目标节点的 DRIVE_ACCOUNT_ID / DRIVE_SPACE_ID
→ 逐字段核对 NODE_ID、账号和空间归属
→ 登录成功后才允许浏览或创建该节点的新挂载
```

规则：

- 禁止把上一台 VPS 的用户名、Cookie、Token、空间 ID 或内存会话直接带到下一台 VPS。
- 即使两个 VPS 恰好存在相同用户名和密码，也必须分别完成目标节点认证，不能依据字符串相同推断为同一账号。
- 本地安全仓库中的普通凭据必须以 `NODE_ID + SSH_HOST_FINGERPRINT + DRIVE_ACCOUNT_ID` 为作用域保存，禁止只按用户名索引。
- 可以列出或填充明确属于目标节点的已保存凭据，但用户仍必须在目标节点登录页主动点击登录；禁止切换后无提示自动登录。
- 目标节点尚未完成完整施工时，登录页必须显示“节点尚未就绪”，普通登录和注册均不可提交，只提供进入高级控制台完成 `[1]` 的入口。
- 目标节点认证失败时停留在目标节点登录页，不恢复旧节点会话，也不显示旧节点文件。
- Windows 上其他节点已经明确挂载的独立盘符可以继续作为各自独立的隧道消费者存在，但绝不能被改指向新节点。新目标节点必须在重新认证成功后使用自己的独立盘符；本版继续禁止单盘符热切换。

### 5.5 高级运维入口

- 点击后按本机设置决定是否弹出 admin 验证。
- 即使当前外层就是以 admin 登录，门禁开启时进入高级控制台仍需重新验证；这是敏感操作的重新认证，不是重复创建账号。
- 普通账号会话也可以看到高级运维入口，但只有另行输入正确的本机 admin 密码后才能进入。
- 验证成功进入现有完整高级控制台。
- 从内层返回时回到原网盘页面，不强制退出普通账号。
- 高级会话锁定不应中断正在进行的普通文件传输。

---

## 6. 内层高级运维控制台

现有导航和有效功能必须保留，包括：

- 总览与全部功能。
- 安装与升级。
- 面板与访问。
- 维护与修复。
- 安全与凭据。
- 备份与报告。
- Windows 本地。
- 流量中心。
- 设备准入。
- 私人网盘管理。
- CDN/XHTTP 和三种代理线路。
- IP 重绑定。
- 拆除施工和恢复基线。

### 6.1 高风险操作的二次认证

无论入口门禁是否关闭，以下操作必须重新验证 admin：

- 显示、复制或导出完整秘密。
- 修改 VPS、面板或网盘密码。
- 导出、恢复或吊销 SSH key。
- 批准 controller。
- 吊销设备。
- 修改 admin 门禁设置。
- 删除备份。
- 永久删除网盘文件。
- 完整拆除节点施工。

### 6.1.1 本机 admin 改密

本机 admin 的改密入口固定放在：

```text
内层高级运维控制台
→ 安全与凭据
→ 本机 admin
→ 修改密码
```

表单必须要求：

```text
当前 admin 密码
新 admin 密码
再次输入新 admin 密码
```

执行规则：

- 当前密码必须真实验证成功。
- 新密码两次输入逐字节一致，不自动修剪或归一化。
- 原子更新日常验证记录和系统可恢复凭据记录。
- 生成新的加密恢复包和恢复码，显示准确保存位置。
- 旧恢复包和旧恢复码立即标记失效。
- 验证新密码能够重新登录外层并通过内层门禁后才提交。
- 任一步失败都恢复旧验证记录、旧系统凭据记录和旧恢复状态。
- 外层账号页、登录页和普通网盘改密表单都不得直接修改 admin 密码。

### 6.2 现有工作流呈现

- 远端脚本日志继续在内层工作流视图显示。
- 图形提示必须使用单次阻塞的输入请求，不允许空输入高速循环。
- 文本问答、是/否、选择框、密码框和文件选择应逐步替换为类型化控件，但功能逻辑和可见反馈不得丢失。
- “安全停止”必须结束当前受管子进程、释放输入等待并进入清理流程。

---

## 7. 代理拓扑状态机

网盘无域名不改变代理拓扑设计。

### 7.1 三种模式

```text
GRAY_ONLY    仅灰云直连
ORANGE_ONLY  仅橙云 CDN/XHTTP
DUAL         灰云直连 + 橙云 CDN/XHTTP
```

### 7.2 菜单 `[1]` 的选择规则

全新或无法识别受管状态的 VPS：

```text
[1] 仅灰云
[2] 仅橙云
[3] 双路
[Q] 取消
```

- 禁止空输入。
- 禁止直接回车采用默认。
- 不显示 `[0]`。

已识别到有效受管状态的 VPS：

```text
[0] 保持当前拓扑
[1] 仅灰云
[2] 仅橙云
[3] 双路
[Q] 取消
```

拆除或损坏状态的选择规则：

- `PROXY_REMOVED_DRIVE_RETAINED`：明确显示“已仅拆代理，网盘和账号将保留”，不显示 `[0]`，必须选择 `[1]`—`[3]` 重新建立代理拓扑。
- 受管拓扑仍可识别但部分组件缺失：显示 `[0] 修复缺失部分并保持台账中的当前拓扑`，同时允许选择 `[1]`—`[3]` 在修复时切换拓扑。
- 已完整恢复为 `BASELINE_UNMANAGED`：作为全新施工，不显示 `[0]`，必须选择 `[1]`—`[3]`。
- 上次拆除中断或状态互相矛盾：先进入拆除恢复判定，不直接显示拓扑默认值；只有恢复到可证明的一致状态后才进入相应选择界面。

### 7.3 必须覆盖的转换

```text
GRAY_ONLY   → ORANGE_ONLY
GRAY_ONLY   → DUAL
ORANGE_ONLY → GRAY_ONLY
ORANGE_ONLY → DUAL
DUAL        → GRAY_ONLY
DUAL        → ORANGE_ONLY
```

还必须覆盖：

- 三种模式各自的同模式幂等重跑。
- 已有节点选择 `[0]`。
- 转换中断后的恢复或回滚。
- 旧构建升级后的状态识别。

### 7.4 Cloudflare 人工向导

仅橙云和双路需要 Cloudflare 指导。向导必须按单步运行：

```text
显示一个人工操作
→ 用户完成后按 Enter
→ 程序执行一个只读验证
→ 通过后进入下一步
→ 输入 Q 取消并进入安全回滚
```

向导不得高速轮询 DNS，不得把本机 TUN/DNS 劫持误判为 Cloudflare 配置错误。

### 7.5 Windows DNS/TUN 预检

菜单 `[1]` 在 DNS 阶段必须区分：

- 系统解析正常。
- 权威/公共解析正常但 Windows 系统解析异常。
- v2rayN TUN 或本地代理可能劫持 DNS。
- DNS 记录确实未传播或填错。

发现本机 TUN 干扰时，明确指导暂停 TUN 后重试；不能要求用户反复删除并重建正确 DNS 记录。

---

## 8. 无域名网盘的远端架构

### 8.0 强制组成与节点状态

所有正常受管代理状态都必须同时包含健康网盘：

```text
MANAGED_GRAY_WITH_DRIVE
MANAGED_ORANGE_WITH_DRIVE
MANAGED_DUAL_WITH_DRIVE
```

不允许作为稳定交付状态存在：

```text
MANAGED_GRAY_WITHOUT_DRIVE
MANAGED_ORANGE_WITHOUT_DRIVE
MANAGED_DUAL_WITHOUT_DRIVE
```

拆除过程中允许存在一个明确的中间/保留状态：

```text
PROXY_REMOVED_DRIVE_RETAINED
```

最终原始基线状态：

```text
BASELINE_UNMANAGED
```

- `PROXY_REMOVED_DRIVE_RETAINED` 表示用户明确选择“只拆代理”，网盘继续工作。
- 只有处于该状态时，“拆除施工和恢复基线”才显示单独拆除剩余网盘的入口。
- 再次运行 `[1]` 时可以在保留网盘和数据的基础上重新安装任一代理模式，恢复为三种正常受管状态之一。
- 诊断发现代理仍在但网盘缺失或不健康时，应标记 `MANAGED_DRIVE_REQUIRED_BROKEN`，要求运行 `[1]` 修复，不能把它当作可接受配置。
- 拆除事务尚未完成时使用 `REMOVAL_IN_PROGRESS`；中断且可依据日志恢复时使用 `REMOVAL_FAILED_RECOVERABLE`；受管资源被人工删除或现场与台账不一致时使用 `MANAGED_COMPONENT_DRIFT`。这些都不是稳定交付状态，必须由 `[1]` 先收敛。
- 网盘服务健康但还没有普通账号时使用账号子状态 `DRIVE_READY_ACCOUNT_REGISTRATION_REQUIRED`；这不是网盘安装失败，外层必须引导注册，不能自动生成用户未知的普通账密。

### 8.1 服务结构

```text
systemd
  └─ text-node-assistant-drive.service
       └─ 非 root 服务用户
            └─ copyparty
                 └─ 127.0.0.1:<managed-drive-port>
```

### 8.2 推荐目录

```text
/opt/text-node-assistant/          程序与固定依赖
/etc/text-node-assistant/drive/    root-only 配置
/var/lib/text-node-assistant/      状态、台账、身份索引
/srv/pna-drive/                     用户文件卷
```

程序、配置、状态和用户数据必须分离。卸载程序不能顺带删除 `/srv/pna-drive/`。

### 8.3 服务账号

- 使用无登录 shell 的独立服务账号。
- 不加入 sudo、docker 或其他高权限组。
- 只对受管文件卷拥有必要权限。
- 普通文件账号由 copyparty/TNA 管理，不创建可 SSH 登录的 Linux 用户。

### 8.4 回环监听硬约束

- 只允许 `127.0.0.1`。
- 禁止 `0.0.0.0`、`::` 和公网 IP。
- 不创建公网 UFW/nftables 网盘放行规则。
- 不安装网盘专用 Nginx 配置。
- 不申请网盘 TLS 证书。

安装和升级后必须通过 `ss`、进程归属和本地 HTTP 健康检查三重验证。只要出现公网监听，施工失败并回滚。

### 8.5 远端内部端口

不得继续假设固定端口永远可用。状态中保存：

```text
DRIVE_BIND_ADDRESS=127.0.0.1
DRIVE_PORT=<managed-port>
DRIVE_SERVICE_ID=<stable-id>
```

分配规则：

1. 有台账时优先验证并复用旧端口。
2. 旧端口被非 TNA 进程占用时，不杀进程，改选新端口。
3. 没有台账时从受管区间选择空闲端口。
4. systemd 启动后确认监听 PID 属于预期服务。
5. 将新端口原子写入 root-only 状态。
6. 失败恢复旧配置和旧服务状态。

---

## 9. SSH 隧道与进程生命周期

### 9.1 隧道类型

```text
PANEL_TUNNEL
DRIVE_BROWSE_TUNNEL
DRIVE_MOUNT_TUNNEL
TRANSFER_TUNNEL
```

实现可以复用同一健康隧道，但注册表必须知道每个消费者，不能因为关闭面板而误杀正在上传的网盘连接。

### 9.2 本地端口选择

- 每次会话选择本地空闲回环端口。
- 只绑定 `127.0.0.1`。
- 使用 `ExitOnForwardFailure` 或等价机制确认 SSH 真正建立转发。
- 端口冲突时有限次数重选，失败后明确报告，不得死循环。
- 不把动态端口当作长期用户配置。

### 9.3 隧道注册表

每个隧道记录：

```text
TUNNEL_ID
NODE_ID
PURPOSE
SSH_PID
LOCAL_PORT
REMOTE_ADDRESS
REMOTE_PORT
CREATED_AT
LAST_HEALTHY_AT
CONSUMER_COUNT
```

### 9.4 关闭顺序

```text
阻止新任务
→ 处理正在传输的任务
→ 卸载相关盘符
→ 释放消费者引用
→ 结束对应 SSH 进程
→ 验证本地端口已经释放
→ 更新界面状态
```

“关闭面板隧道”和“关闭网盘隧道”必须真实有效；不能只能依赖“安全停止”。

---

## 10. 文件传输与 Windows 挂载

### 10.1 传输通道

由于网盘不经过 Cloudflare，不受 Cloudflare 单请求体上限影响。仍需为大文件提供：

- 分块上传。
- 断点续传。
- 校验。
- 暂停和继续。
- 网络断线后的隧道重建。
- 程序重启后的任务恢复。

可采用 copyparty up2k/u2c 兼容协议、受审计的内置实现或固定版本辅助程序，但秘密不能出现在命令行参数、日志或长期明文临时文件中。

### 10.2 Windows 挂载

- 使用固定并校验版本的 rclone 与 WinFsp。
- 默认提供只读挂载作为稳定模式。
- 可选提供小文件读写挂载，并明确缓存和未关闭文件风险。
- 大文件上传优先走 TNA 传输队列，不依赖资源管理器长时间写入。
- 每个 VPS 使用独立盘符，禁止本版实现单盘符无缝抢占。
- 选择新的 VPS 进行挂载时必须先执行第 5.4.1 节的目标节点重新认证；不得沿用当前外层会话直接挂载另一节点。

示例：

```text
节点 A → P:
节点 B → Q:
节点 C → R:
```

### 10.3 Android

Android 不提供盘符挂载，提供原生浏览、上传、下载、任务队列和节点切换。

---

## 11. 多 VPS 模型

- 每台 VPS 拥有稳定 `NODE_ID`。
- 每台 VPS 的网盘目录、配额、节点级普通账号、设备访问身份、远端端口和施工台账相互独立。
- 不把多个 VPS 合并为统一文件树。
- 每个传输任务永久绑定创建时的 `NODE_ID`。
- 切换当前节点不迁移、重定向或重命名旧任务。
- 已挂载节点继续保持原盘符，除非用户明确卸载。
- 节点历史支持快速选择和一键删除非秘密历史。
- 节点历史只用于快速选择目标地址，不能携带或恢复上一节点的网盘会话。每次切换当前节点都必须回到标有目标 `NODE_ID` 和 Host 指纹的登录界面重新认证。

---

## 12. 设备准入 v2

### 12.1 目标

一次准入同时交付：

- 设备签名身份。
- 独立 SSH 公钥登录。
- 独立 VLESS。
- 访问回环网盘登录/注册端点的设备能力。
- 对应节点的非秘密元数据。

设备准入不自动创建或绑定普通网盘账号。准入完成后，新设备可以在外层输入节点上已经存在的普通账密访问原空间，也可以自行注册新的普通账号。

### 12.2 邀请状态机

```text
CREATED
  → LOCKED_TO_DEVICE
  → APPROVED_PENDING_VERIFY
  → VERIFIED
  → CONSUMED
```

补充终止状态：

```text
EXPIRED
CANCELLED
REVOKED
```

### 12.3 核心规则

- 邀请由 controller 创建。
- 新设备无需先 SSH 登录 VPS。
- 新设备离线生成自己的 SSH key 和签名 key。
- 首个合法响应锁定邀请，第二台设备被拒绝。
- 批准后先安装新设备 SSH 公钥，再创建独立 VLESS 和回环网盘访问能力。
- 邀请必须在新设备实际 SSH key 登录、验证设备访问能力并成功到达网盘登录/注册端点后才消费；不要求为该设备另建普通网盘账号。
- 中途断线保持 `APPROVED_PENDING_VERIFY`，下次可继续。
- 不能因为一次网络抖动要求从头重建邀请。

### 12.4 离线交换

支持：

- 二维码。
- PNG/SVG 导出。
- 文本复制。
- 邀请文件和响应文件。

内容只包含完成准入所需的公开信息、nonce、过期时间和签名，不包含私钥、VPS 密码、面板密码或现成网盘密码。

### 12.5 吊销

吊销一台设备必须分别撤销：

- SSH authorized key。
- VLESS 身份。
- 访问网盘回环端点的设备能力。
- 未完成邀请和领取状态。
- 本地受管记录。

不得删除节点级普通网盘账号或空间，不得影响其他设备继续使用同一普通账密。最后一个 active controller 受保护。

---

## 13. 菜单 `[1]` 的完整施工流程

### 13.1 本地前置

```text
检查本地 admin 状态
→ 检查 Windows OpenSSH
→ 缺失时一次性安装并验证
→ 检查本地网络、TUN 和 DNS 状态
→ 选择节点历史或手工输入
→ 校验 SSH Host Key
→ 选择临时密码或长期 key
```

OpenSSH 安装不得陷入“检测—安装—重启—再次检测”的无限循环。一次失败后报告准确原因和人工步骤。

### 13.2 远端版本判断

```text
未安装                → 安装当前 v0.9.5 内部构建
同版本同构建完整      → 跳过上传和 bootstrap
同版本旧构建          → 仅升级工具包构建
旧版本                → 备份、升级、验证、清理旧程序
远端构建更新          → 拒绝降级，提示更换客户端
同版本但文件不完整    → 读取台账和现场证据，在 [1] 内生成有限修复计划
```

### 13.2.1 拆除结果与缺失组件识别

版本判断之后、上传或 bootstrap 之前，`[1]` 必须同时读取：

```text
远端施工台账
最后一次拆除事务状态
最后一次拆除模式和提交标记
拆除报告 / 救援包摘要（如存在）
各受管资源的当前路径、哈希、服务状态、端口和依赖
本地保存的对应 NODE_ID 拆除回执（远端已恢复基线时使用）
```

拆除事务至少记录：

```text
REMOVAL_TRANSACTION_ID
REMOVAL_MODE=PROXY_ONLY|FULL_BASELINE|REMAINING_DRIVE
REMOVAL_STATUS=PLANNED|IN_PROGRESS|COMMITTED|FAILED_RECOVERABLE
REMOVED_RESOURCE_IDS
PRESERVED_RESOURCE_IDS
DATA_EXPORT_RESULT
BASELINE_RESTORE_RESULT
LAST_VERIFIED_AT
```

识别和恢复矩阵：

| 现场状态 | `[1]` 必须执行的动作 |
|---|---|
| 三种完整 `*_WITH_DRIVE` | 幂等检查；按用户选择保持或转换拓扑 |
| `PROXY_REMOVED_DRIVE_RETAINED` | 保留网盘、账号、空间、文件、设备和 SSH；只重装所选代理拓扑及其依赖 |
| `MANAGED_DRIVE_REQUIRED_BROKEN` | 从台账恢复网盘服务、回环端口、配置和账号索引；不得覆盖文件或重建已有账号 |
| `MANAGED_COMPONENT_DRIFT` | 展示缺失/被改动资源清单，只恢复有归属证据的 TNA 组件；用户后来修改的资源进入冲突处理 |
| `REMOVAL_IN_PROGRESS` / `REMOVAL_FAILED_RECOVERABLE` | 依据事务日志先回滚到拆除前一致状态，或完成到已选择的拆除目标；不得边拆边装 |
| `BASELINE_UNMANAGED` 且完整拆除已提交 | 按全新节点重新施工；不声称能够自动恢复已确认永久删除的数据或旧账号 |
| 无台账但发现疑似旧组件 | 进入谨慎接管，只读识别并要求用户确认归属；禁止盲删、盲覆盖或伪造恢复结果 |

`[1]` 必须在界面显示“检测到的保留项、缺失项、冲突项、计划恢复项和明确不触碰项”，用户确认后才执行。恢复计划必须以资源 ID 为单位，不能以“重新跑完整安装脚本”代替识别。

如果仅拆代理后重新施工，以下对象必须逐项核对为原对象而不是新建副本：

```text
ADMIN_SPACE_ID
现有 DRIVE_ACCOUNT_ID / DRIVE_SPACE_ID
普通账号密码哈希和 root-only 加密托管记录
/srv/pna-drive/ 文件树
active 设备身份和 controller
网盘回环端口与配额状态
```

任何对象身份发生意外变化都视为恢复失败并回滚。只有新代理拓扑及其明确依赖允许重新创建。

### 13.3 拓扑与网盘

```text
读取受管拓扑
→ 读取拆除回执、施工台账和现场资源证据
→ 将节点收敛到可证明的一致恢复起点
→ 生成并确认缺失组件恢复计划
→ 强制选择或保持灰云/橙云/双路
→ 收集对应代理域名和邮箱
→ 执行可回滚拓扑事务
→ 安装或升级回环网盘服务
→ 创建/迁移 ADMIN_SPACE_ID
→ 启用受信设备可用的外层登录/注册端点
→ 使用内部验收身份执行登录和 CRUD 健康检查
```

网盘步骤不询问域名、邮箱、证书或 Cloudflare 配置。

网盘步骤是强制事务阶段：

- 不显示“是否安装网盘”。
- 不允许跳过或以后再装。
- 已安装时执行版本、监听、账号、配额和 CRUD 验收，而不是重复安装。
- 缺失或落后时由 `[1]` 安装/升级。
- 比客户端构建更新时遵循整体防降级规则。
- 网盘失败时整次 `[1]` 标记为未完成，不生成成功交接，不开放面板后续步骤。
- 如果代理阶段已经提交而网盘阶段失败，事务必须恢复到施工前状态或进入有明确恢复点的失败状态，不能留下无台账半成品。
- 拆除后恢复期间必须保持 `DRIVE_REGISTRATION_READY=0`；只有全部缺失部分恢复、身份一致性检查和最终验收通过后才能重新开放注册。
- 只有整次 `[1]` 最终提交、节点进入三种 `*_WITH_DRIVE` 稳定状态且远端发布 `DRIVE_REGISTRATION_READY=1` 后，外层注册端点才允许创建普通账号。此前即使网盘进程已经启动，也必须拒绝注册。

普通网盘账号不由 `[1]` 静默替用户创建。网盘服务首次就绪但节点尚无普通账号时，客户端进入：

```text
DRIVE_READY_ACCOUNT_REGISTRATION_REQUIRED
```

随后返回外层并突出显示“注册普通网盘账号”。用户完成外层注册、真实登录和 CRUD 验收后，生成包含真实 `DRIVE_ACCOUNT_ID`、`DRIVE_SPACE_ID`、用户名和密码的账号交接单。已有普通账号时，其他受信设备直接在外层登录同一账号，不重复注册。

### 13.4 成功交付顺序

```text
读取并验证完整交接单
→ 显示真实凭据
→ 用户确认已保存
→ 询问是否清空含秘密剪贴板
→ 询问是否清理多余远端备份（默认 N）
→ 询问是否打开 3x-ui 面板
→ 返回或进入网盘前台
```

任何一步失败都不得使用 SSH 的 `Connection closed.`、空输出或错误摘要冒充交接单。

---

## 14. 自适应网盘容量

### 14.0 三空间模型

单台 VPS 的容量模型固定按最多 3 个隔离空间设计：

```text
空间 1：ADMIN_SPACE_ID
空间 2：普通账号 A 的 DRIVE_SPACE_ID
空间 3：普通账号 B 的 DRIVE_SPACE_ID
```

这只是逻辑配额，不预先创建三个固定磁盘分区。普通账号尚未注册时不创建空目录或假账号，但容量计算仍为尚未创建的可用槽位保留最低安全余量。

### 14.1 输入因素

- 磁盘总容量。
- 当前可用容量。
- 系统保留空间。
- 已有网盘文件。
- 上传临时文件。
- 当前备份占用。
- 用户设定的硬上限。
- 当前已经创建的普通账号数量和最多 3 个空间的保底份额。

### 14.2 安全原则

- 先扣除系统、备份、上传临时文件和安全余量，得到 `SAFE_DRIVE_POOL`；默认保底额度为 `floor(SAFE_DRIVE_POOL / 3)`，三个空间采用相同的最低平均水准。
- 单空间初始额度仍以 2 GiB 为最低可用目标；如果安全池连三个最低目标都无法承担，必须显示容量不足并拒绝新增普通账号，不能侵占系统保留空间。
- 保底平均额度不是永久硬上限。某空间接近额度且其他空间实际占用较低时，可以按受控步长自适应拉高，但必须保留其他已存在空间的写入余量，以及尚未注册普通账号槽位的最低注册余量。
- 自适应提高只增加逻辑配额，不承诺无限容量；每次提高前都必须重新读取实际磁盘可用量、备份占用和上传临时文件。
- 当三个空间都已存在时，额外安全容量按当前实际用量和增长需求动态分配；不得因为 admin 或先注册账号创建得早，就永久吞掉其余空间的保底份额。
- 20 GiB 小盘应至少为系统保留约 6 GiB 或 30%，取更安全值。
- 配额减少不得删除文件。
- 已用空间超过新额度时只暂停新增写入，读取和导出仍可用。
- 任何计算异常采用旧额度，不得写入 0 或无限值。

### 14.3 修改事务

```text
计算候选额度
→ 记录旧额度和文件统计
→ 备份配置
→ 写入候选额度
→ 重载服务
→ 执行边界探测
→ 成功提交 / 失败回滚
```

---

## 15. 施工台账

### 15.1 为什么是前置功能

没有台账就无法可靠判断资源是用户原有、TNA 新建还是施工后又被用户修改。因此台账和回滚框架必须早于新增网盘施工。

### 15.2 资源归属

```text
PREEXISTING
CREATED_BY_TNA
MODIFIED_BY_TNA
ADOPTED_BY_TNA
USER_CHANGED_AFTER_TNA
```

### 15.3 每项资源记录

```text
RESOURCE_ID
资源类型
绝对路径或系统标识
首次发现时间
施工前状态
施工前哈希
施工前备份路径
施工后哈希
当前哈希
归属状态
安装动作
验证动作
回滚动作
拆除动作
依赖资源
```

施工台账还必须把每次拆除的“被删除资源”和“明确保留资源”作为不可混淆的两组集合持久化，并生成一份按 `NODE_ID` 关联的本地拆除回执。远端整体拆除会删除远端 TNA 状态，因此 `[1]` 只能用本地回执说明这是一台曾被完整恢复基线的节点；回执不能用来伪造或复活已经永久删除的网盘数据和账号。

覆盖范围至少包括：

- 软件包。
- 二进制和工具包目录。
- systemd 服务。
- 系统用户和组。
- Nginx 配置。
- 证书。
- UFW/nftables/iptables 规则。
- fail2ban 配置。
- cron 和 logrotate。
- 3x-ui/Xray 入站和订阅。
- WARP 路由。
- SSH authorized keys。
- copyparty 配置、账号和文件卷。
- 运行态元数据和凭据仓。

### 15.4 用户后续修改冲突

当前哈希与施工后哈希不一致时，不允许静默覆盖。必须：

1. 标记 `USER_CHANGED_AFTER_TNA`。
2. 保存当前文件副本。
3. 显示冲突。
4. 仅在用户明确选择后恢复旧版或保留新版。

---

## 16. 拆除施工和恢复基线

### 16.1 入口与权限

- 位于内层高级运维控制台。
- 进入前或执行前必须验证 admin。
- 只执行预检、备份、停用、卸载、恢复和删除。
- 禁止上传工具包、bootstrap 或重新安装。

### 16.2 按状态动态显示拆除动作

正常受管状态，即代理和强制网盘都存在时，只显示：

```text
[1] 仅拆除代理施工（强制保留网盘、文件和 TNA 管理能力）
[2] 整体拆除全部 TNA 施工并恢复原始基线
[0] 取消
```

禁止在该状态显示：

```text
停用网盘
卸载网盘
删除网盘
保留文件但拆网盘
```

选择“仅拆除代理”并验证完成后，节点进入：

```text
PROXY_REMOVED_DRIVE_RETAINED
```

只有识别到该状态，“拆除施工和恢复基线”才显示网盘拆除入口：

```text
[1] 拆除剩余网盘和 TNA 管理施工，恢复原始基线
[0] 取消
```

此时再次运行菜单 `[1]` 也可以保留现有网盘和文件，重新安装任一代理模式。

重新安装时 `[1]` 必须明确报告：

```text
检测到：仅代理已拆除
将保留：网盘文件、admin 空间、两个普通账号上限内的现有账号、设备准入和 SSH
将恢复：用户新选择的代理拓扑及其受管依赖
不会执行：重建网盘、清空空间、轮换普通账号、覆盖现有 DEVICE_ID
```

已经是 `BASELINE_UNMANAGED` 时不显示可执行拆除项，只报告“没有检测到 TNA 受管施工”。

### 16.3 仅拆代理的保留范围

仅拆代理必须保留并复核：

- 回环网盘服务。
- `/srv/pna-drive/` 全部用户文件。
- `ADMIN_SPACE_ID`。
- 全部节点级普通网盘账号、`DRIVE_SPACE_ID` 和账号凭据托管密文。
- 各 active 设备访问网盘回环端点的能力。
- 网盘 SSH 隧道能力。
- TNA 工具包、施工台账和后续重新安装代理所需管理能力。
- SSH 登录、设备 controller 和厂商 VNC/串口救援能力。

仅拆代理必须撤销或恢复：

- TNA 新建的代理入站和订阅。
- 灰云/橙云/CDN/XHTTP 路径。
- 代理专用 Nginx、证书、WARP 路由和防火墙规则。
- TNA 新装的 3x-ui/Xray，仅在台账证明它们不是安装前已有组件时移除；原有组件恢复基线。

完成后必须验证代理入口已经消失、网盘仍可通过 SSH 隧道完成登录和最小读写。

### 16.4 整体拆除与恢复基线

整体拆除意味着直接收敛到 `BASELINE_UNMANAGED`。不能以“网盘服务还在”或“用户文件还留在 TNA 目录”为完成状态。

由于整体拆除会移除网盘和 TNA 文件卷，执行前必须：

1. 统计并显示网盘文件数量、总大小和绝对目录。
2. 提供将网盘数据导出到用户选择位置的步骤。
3. 对导出结果执行数量、大小和抽样哈希验证。
4. 如果用户选择不导出，必须明确确认整体拆除将永久删除 TNA 网盘数据。
5. 任意确认不完整时取消整体拆除，不得留下半拆状态。

完整顺序：

```text
只读预检
→ 展示准确拆除清单和保留清单
→ 创建并下载救援包
→ 校验救援包哈希和内容
→ 导出并验证网盘数据，或取得明确永久删除确认
→ 停止新任务
→ 卸载盘符并关闭网盘隧道
→ 撤销普通设备
→ 拆除网盘服务和受管代理组件
→ 删除 TNA 文件卷和凭据托管密文
→ 恢复施工前配置
→ 验证备用 SSH/VNC 路径
→ 最后撤销当前 controller
→ 删除远端 TNA 工具包和状态
→ 独立复核
→ 生成拆除报告
```

SSH 维护通道必须最后处理，不能先把自己锁在 VPS 外。

### 16.5 整体拆除的数据删除保护

未导出数据时，整体拆除的永久删除确认必须同时满足：

- admin 再验证。
- 显示将删除的绝对目录和估算大小。
- 输入节点显示名称。
- 输入明确删除确认短语。
- 再次确认。
- 不接受空输入或默认 Yes。

已经从“仅拆代理”状态进入“拆除剩余网盘并恢复基线”时，采用完全相同的数据导出和永久删除保护，不提供绕过确认的快捷卸载。

### 16.6 软件包策略

- 默认不运行 `apt autoremove`。
- TNA 专用二进制可以按台账删除。
- Nginx、curl、unzip、fail2ban 等通用包默认保留。
- 只有台账证明由 TNA 新装、没有其他依赖且用户明确要求时，才允许卸载通用包。

### 16.7 “恢复原状”的定义

恢复原状指恢复到 TNA 首次接管时有证据记录的基线，并移除 TNA 后来创建的代理、网盘、文件卷、身份、服务、规则和工具包。它不承诺把复杂已有 VPS 变回厂商刚交付的空系统；真正恢复厂商原厂状态只能使用厂商重装系统功能。

---

## 17. 备份与恢复

### 17.1 两类备份分离

配置备份：

- 程序、配置、身份索引、台账、服务定义。
- 不默认复制整个网盘文件卷。

数据备份：

- 用户明确选择后导出网盘文件。
- 显示体积、目标位置和预计风险。
- 不能假装 20 GiB VPS 可以在本机磁盘不足时完成全量导出。

### 17.2 清理多余备份

- 菜单 `[1]` 成功后继续保留可选清理步骤。
- 默认 `N`。
- 明确选择 `Y` 后，先创建并验证一份当前配置备份，再精确删除已知旧备份。
- 不能删除未知文件名、用户手工备份或网盘数据。

### 17.3 恢复

- 图形界面必须允许选择备份，不能只在日志里打印路径后等待无法输入的文本。
- 恢复前展示来源、节点、时间、哈希和包含内容。
- 新配置验证成功后才替换旧配置。
- SSH key 恢复必须先真登录验证，再修改绑定状态。

---

## 18. 错误、取消和恢复语义

### 18.1 失败即停止

以下情况必须停止当前分支：

- SSH/Host Key 验证失败。
- 远端命令退出码非零。
- 缺少结构化成功标记。
- Credential Handoff 为空或字段不全。
- 面板元数据为空。
- 网盘健康检查失败。
- 新账号真实登录失败。
- 回环服务意外公网监听。
- 拓扑转换验证失败。

停止后不得继续：

- 复制剪贴板。
- 打开面板或网盘。
- 清理备份。
- 删除旧凭据。
- 消费设备邀请。

### 18.2 安全停止

第一次点击：

- 请求当前工作流有序取消。
- 给 SSH、scp、rclone、上传程序和远端脚本有限清理时间。
- 关闭对应输入请求。

第二次或超时：

- 结束受管进程树。
- 卸载盘符。
- 释放隧道。
- 标记任务为可恢复或已取消。

界面不得无限停留在“正在等待清理”。

### 18.3 SSH 远端包装

- stdout、stderr 和 SSH 客户端诊断必须分离解释。
- `Connection closed.` 不是业务输出。
- 远端返回值必须通过可靠标记和 SSH 退出码共同判断。
- 交接单、面板元数据和状态必须有开始/结束边界、格式校验和非空校验。

---

## 19. Windows 界面结构

### 19.1 外层导航

建议固定为：

```text
网盘首页
文件
传输任务
挂载
节点
账号
高级运维
```

账号/安全页面必须提供：

```text
查看本机 admin 恢复状态
打开恢复包目录
另存新的加密恢复包
验证恢复包和恢复码
前往高级运维控制台修改 admin 密码
查看当前恢复包创建时间
```

界面不得只显示“已保存”，必须显示系统凭据记录状态和恢复包的准确绝对路径；不得在未完成系统身份确认或 admin 验证时直接显示真实密码。外层这里只提供跳转，不得内嵌 admin 新密码输入框。

### 19.2 内层导航

保留现有风格和功能分组：

```text
[00] 总览 / 全部功能
[01] 安装与升级
[02] 面板与访问
[03] 维护与修复
[04] 安全与凭据
[05] 备份与报告
[06] Windows 本地
设备准入
私人网盘管理
流量中心
拆除施工和恢复基线
```

### 19.3 视觉与可用性

- 保持硬核运维风格，但信息层级清晰。
- 所有滚动条、输入框、下拉框、密码框和对话框使用统一主题。
- 不允许纯日志铺满页面代替结构化表单。
- 长任务保持界面可响应，禁止 UI 线程忙循环导致 CPU 吃满。
- 输入提示一次只产生一个活动请求。
- Enter、Y/N、直接回车和取消按钮必须与当前提示状态一致。

---

## 20. Android 结构

Android 与 Windows 使用同一状态语义，但界面适配移动端。

必须提供：

- 普通账号或本机 admin 登录外层。
- 节点历史和快速切换。
- 文件浏览、上传、下载和任务队列。
- SSH 隧道前台服务和明确通知。
- 新设备邀请响应。
- 普通网盘凭据安全保存。
- 进入高级运维前的 admin 门禁。
- 中文完整本地化。

Android 不提供：

- Windows 盘符挂载。
- 隐藏后台永久隧道。
- 依赖外部终端应用才能完成的核心流程。

密码认证失败必须区分：密码错误、服务器禁止密码登录、Host Key 变化、网络断开和 SSH 会话被远端关闭。

---

## 21. 供应链与隐私

### 21.1 第三方组件

所有内嵌或下载组件必须固定：

- 版本。
- 官方来源。
- 文件大小。
- SHA-256。
- 许可证和归属说明。

适用组件包括 copyparty、rclone、WinFsp 和 Android SSH 依赖。

### 21.2 下载策略

- 优先由用户确认后从固定官方 URL 下载并校验。
- 下载失败不得改用未校验镜像。
- 不允许关闭 TLS 或包签名验证。
- 不使用 `trusted=yes`、`--allow-unauthenticated` 等绕过手段。

### 21.3 发布前隐私扫描

必须扫描：

- 源码树。
- Git 历史待发布差异。
- EXE/APK 解包内容。
- Linux 工具包解包内容。
- 文档和测试夹具。
- PDB、资源、字符串表和构建日志。

禁止出现任何开发者或测试者的真实 VPS IP、域名、邮箱、密码、Token、私钥或面板凭据。

---

## 22. 迁移策略

### 22.1 本地迁移

- 识别旧节点历史、长期 key、撤销备份、Credential Manager 项和 Android 加密仓库。
- 迁移前创建本地可恢复快照。
- 不要求用户重新输入仍可安全读取的非秘密元数据。
- 不把旧明文秘密复制到新普通配置。

### 22.2 远端网盘迁移

当前早期网盘可能使用固定 `127.0.0.1:3923`、单账号和固定配额。迁移流程：

```text
只读检测
→ 备份旧配置
→ 识别文件卷
→ 创建稳定 ADMIN_SPACE_ID
→ 将旧账号登记为节点级 DRIVE_ACCOUNT_ID / DRIVE_SPACE_ID
→ 在旧服务仍可恢复时验证新账号
→ 切换到受管动态回环端口
→ 验证文件数量和 CRUD
→ 提交迁移
```

不得移动或删除文件卷来“重新初始化”。

### 22.3 旧设备准入迁移

- 保留现有有效 VLESS，先登记为 legacy 设备。
- controller 完成 v2 身份补全后再允许逐设备升级 SSH 和网盘凭据。
- 新身份验证成功前不撤销旧身份。

### 22.4 无台账节点

- 进入谨慎接管模式。
- 能根据 TNA 标记、路径、哈希和服务名证明归属的资源可以登记。
- 无法证明归属的资源只报告，不自动删除。

---

## 23. 施工顺序

正式动工时按以下顺序进行，每阶段完成后更新本文件的状态表。

### 阶段 A：台账与事务框架

- 状态 schema。
- 资源归属。
- 备份、提交、回滚接口。
- 旧节点谨慎接管。

### 阶段 B：拆除施工和恢复基线基础

- 只读预检。
- 分级拆除。
- 数据保留默认。
- 救援包和独立复核。

### 阶段 C：本地身份根

- 首次创建 admin。
- Windows Credential Manager / Android Keystore 可恢复 admin 记录。
- 加密恢复包、恢复码、另存和恢复流程。
- Windows/Android 安全仓库。
- 入口门禁和高风险二次验证。

### 阶段 D：设备准入 v2

- 邀请锁定。
- 中断续跑。
- SSH、签名、VLESS、网盘登录/注册端点访问能力统一交付。
- 独立吊销。

### 阶段 E：远端无域名网盘

- 非 root 服务。
- 动态回环端口。
- `ADMIN_SPACE_ID`。
- 外层节点级普通账号注册。
- 同一普通账号跨全部受信设备访问同一 `DRIVE_SPACE_ID`。
- 自适应配额。
- 真实 CRUD 验收。

### 阶段 F：Windows 双层客户端

- 外层普通登录。
- 文件浏览和传输队列。
- 内层完整高级控制台嵌入。
- admin 门禁。
- 隧道注册表。

### 阶段 G：Windows 挂载

- rclone/WinFsp 固定供应链。
- 独立盘符。
- 卸载和异常清理。

### 阶段 H：Android 对齐

- 普通网盘前台。
- 准入、隧道和传输。
- 内层高级控制台入口。
- 中文本地化。

### 阶段 I：代理拓扑联合收敛

- 三模式首次选择。
- 六种互切。
- `[0]` 保持。
- DNS/TUN 预检。
- 网盘与拓扑解耦验证。

### 阶段 J：成熟产品封口

- 管理面威胁模型和严格 SSH 模式。
- 节点级全局操作协调器、租约与 fencing token。
- 文件原子提交、并发冲突、回收区、磁盘和 inode 压力处理。
- 真实数据备份状态、恢复演练和本地卸载/设备退役。
- 会话限速、超时、退出全部设备和 controller 加密凭据托管。
- 签名发布清单、内部 Build ID、SBOM、第三方许可和结构化脱敏日志。
- 支持平台、可用性和现有高级功能回归合同。

### 阶段 K：软件内新手引导

- 状态驱动的引导模式和专家模式。
- 新 VPS、已有节点和设备邀请三条首用分支。
- Cloudflare 单步操作、单步验证和安全取消。
- 每一步固定说明模板、失败修复和进度恢复。
- 离线帮助中心和第一次使用者可完成性测试。

### 阶段 L：发布验收

- 全平台构建。
- 静态和单元测试。
- 干净 VPS 安装。
- 旧节点升级。
- 拆除和重装。
- 隐私解包扫描。
- 成熟产品封口测试和新手独立完成测试。

---

## 24. 测试矩阵

### 24.1 安装与版本

- 新电脑没有任何节点时，外层显示 `NO_BOUND_NODE` 和“未挂载节点”，并可进入高级控制台。
- 未挂载状态不显示假文件、假容量或普通密码错误。
- 全新 VPS 安装。
- 同构建重跑。
- 同版本旧构建升级。
- 旧版本升级。
- 远端更新构建拒绝降级。
- 同版本文件缺失时，有充分台账和归属证据则由 `[1]` 生成并执行有限修复；证据不足或存在资源冲突时安全停止，不要求另找修复入口。
- 仅 `[1]` 能安装工具包。
- 仅拆代理后运行 `[1]`，只恢复所选代理拓扑，网盘文件、空间、账号和设备身份逐字段保持不变。
- 完整拆除并恢复基线后运行 `[1]`，进入全新施工流程且不声称恢复已永久删除数据。
- 拆除在每个步骤中断后运行 `[1]`，先恢复或完成拆除事务到一致状态，再开始安装；不存在边拆边装。
- 人工删除单个受管组件后运行 `[1]`，只修复有归属证据的缺失组件，不覆盖 `USER_CHANGED_AFTER_TNA` 资源。

### 24.2 拓扑

- 三种模式首次安装。
- 六种转换。
- 三种同模式幂等。
- 已有节点 `[0]`。
- 每个阶段取消、断网和回滚。
- v2rayN TUN 开启与关闭两种 DNS 环境。

### 24.3 网盘

- 服务只监听 `127.0.0.1`。
- 预设端口被其他程序占用。
- 动态端口迁移。
- 普通账号登录成功和失败。
- 受信设备可以在外层注册节点级普通网盘账号。
- 注册入口直接位于初始登录界面，不要求先登录。
- 第一个和第二个普通账号注册成功；第三个注册请求返回 `DRIVE_ACCOUNT_LIMIT_REACHED`，且没有残留账号、目录、哈希或托管记录。
- 一个普通账号在多台受信设备登录仍只占一个账号名额。
- 固定 admin 空间加两个普通空间时总空间数恰好为 3。
- 未挂载或未受信设备无法注册，且得到准确引导。
- 未施工、半施工、升级中、回滚中、施工失败、`*_WITHOUT_DRIVE`、`PROXY_REMOVED_DRIVE_RETAINED` 和 `BASELINE_UNMANAGED` 均无法新注册普通账号。
- 只有三种完整 `*_WITH_DRIVE` 状态、最近施工已提交且实时健康验收通过时返回 `DRIVE_REGISTRATION_READY=1`。
- 在注册事务的每个故障注入点都不存在本地或远端幽灵账号；重新启动客户端和 VPS 后结论一致。
- 注册失败时账号、空间、哈希、托管记录和用户名预留完整回滚。
- 设备 A 注册的普通账号可以在设备 B 登录并看到相同 `DRIVE_SPACE_ID` 和文件。
- 吊销设备 A 后，设备 B 仍可继续使用同一普通账号；普通账号本身不被删除。
- 普通账号真实用户名和密码进入完整交接单。
- 交接单普通网盘凭据与实际登录凭据逐字段一致。
- 旧账号只剩哈希时拒绝生成残缺交接单，并在显式轮换后恢复完整交付。
- 普通账号在外层使用正确旧密码和两次相同新密码改密成功。
- 普通账号旧密码错误、新密码两次不一致和新密码验真失败时完整回滚。
- 普通账号改密成功后交接单、本地安全仓库和 root-only 加密托管记录同步更新。
- 普通账号改密成功后旧密码和旧会话失效。
- 匿名访问拒绝。
- 上传、下载、重命名、删除和删除后复查。
- 大文件中断续传。
- 程序重启后恢复任务。
- 配额边界和磁盘不足。
- 三个空间默认取得相同保底平均额度；单空间自适应拉高后，其他空间和未注册槽位的最低安全余量仍然存在。
- 三种正常代理模式均强制存在健康网盘。
- 网盘安装失败时 `[1]` 整体不交付成功。
- 正常受管状态不显示网盘单独拆除入口。

### 24.4 隧道和挂载

- 面板隧道与网盘隧道同时运行。
- 关闭面板不影响网盘。
- 关闭网盘不影响面板。
- 本地端口冲突有限重试。
- SSH 进程意外退出。
- rclone 未关闭文件。
- 正常卸载、强制停止和程序崩溃后的清理。
- 多节点独立盘符。
- 从节点 A 切到节点 B 时退回带节点 B 身份的登录页，节点 A 的账号会话不能访问节点 B。
- 节点 A、B 使用相同用户名和密码时仍分别完成认证，并得到各自正确的 `NODE_ID`、`DRIVE_ACCOUNT_ID` 和 `DRIVE_SPACE_ID`。
- 切换认证失败后不显示节点 A 的旧文件，也不把节点 A 的独立盘符改指向节点 B。
- 目标节点未完成完整施工时，浏览、挂载和注册同时被阻止并准确引导 `[1]`。

### 24.5 admin 与普通账号

- 首次密码两次一致/不一致。
- Windows Credential Manager / Android Keystore 中存在可恢复 admin 凭据记录。
- 创建 admin 后生成加密恢复包并显示准确绝对路径。
- 使用同一系统用户恢复 admin。
- 使用恢复包和恢复码在重装/迁移场景恢复 admin。
- 错误恢复码、损坏恢复包和过期恢复包安全失败。
- 修改 admin 密码后旧恢复包失效、新恢复包生效。
- 外层没有直接修改或重置 admin 密码的表单。
- admin 只能在内层“安全与凭据”输入旧密码和两次新密码改密。
- admin 改密失败时验证器、系统凭据和恢复包状态完整回滚。
- 普通账号无法进入高级功能。
- admin 门禁默认开启。
- 内层关闭、开启门禁。
- 高风险动作始终二次认证。
- 高级会话超时不影响普通传输。

### 24.6 设备准入

- 邀请创建和离线响应。
- 首台设备锁定。
- 第二台设备抢占失败。
- 批准后网络中断。
- SSH 登录成功但网盘登录/注册端点能力验证失败。
- 续跑后成功消费。
- 暂停、恢复和吊销。
- 最后 controller 保护。
- 单设备吊销不影响其他设备。

### 24.7 拆除施工和恢复基线

- 正常受管状态只显示“仅拆代理”和“整体拆除”。
- 仅拆代理后网盘、文件、账号、SSH 和 TNA 管理能力继续可用。
- 仅拆代理后节点准确进入 `PROXY_REMOVED_DRIVE_RETAINED`。
- 只有 `PROXY_REMOVED_DRIVE_RETAINED` 显示“拆除剩余网盘并恢复基线”。
- 整体拆除直接收敛到 `BASELINE_UNMANAGED`，不残留 TNA 网盘服务或文件卷。
- 整体拆除前的数据导出和抽样校验。
- 用户拒绝导出时的永久删除多重确认。
- 用户修改配置冲突。
- 救援包损坏时拒绝继续。
- 拆除完成后重新运行 `[1]` 安装。

### 24.8 隐私与供应链

- 密码不进日志和命令行。
- `Connection closed.` 不进入交接单。
- 空 handoff 不复制。
- EXE/APK/工具包解包扫描。
- 第三方哈希错误安全停止。
- 无真实测试节点资料。

### 24.9 成熟产品封口

- 两台 controller 同时发起 `[1]`、拆除、恢复、改密和设备变更时，全局操作协调器只允许一个破坏性事务提交。
- 锁持有者崩溃、租约超时、接管和旧 fencing token 迟到提交均按设计处理。
- SSH 严格模式启用前验证 controller 真登录和厂商救援路径；启用后未批准设备密码和未知 key 均无法登录。
- TNA 管理的 authorized key 精确增删，用户原有 key 不被覆盖；Host Key 轮换进入高风险确认。
- 上传断电、磁盘满、inode 满、重复续传、校验失败和两设备同名并发写入不产生假完成或静默覆盖。
- 路径穿越、绝对路径、符号链接逃逸和跨账号空间访问全部拒绝。
- 回收区保留、永久删除、配额统计和安全水位行为一致。
- 未配置异机备份时准确显示未保护；空 VPS 完整恢复演练通过。
- 账号登录限速、会话超时、改密失效、退出当前设备和退出所有设备通过。
- 服务器只取得普通账号托管密文时不能独立还原密码；controller 丢失时准确进入不可恢复/轮换分支。
- EXE、APK、工具包、签名清单、Build ID、SBOM 和第三方许可逐项一致。
- 本机四种卸载/清理分支、最后 controller 保护、隧道/盘符/后台进程清理通过。
- 日志轮转、脱敏、支持包和零遥测默认通过隐私检查。
- 声明支持的每个 OS/架构均完成安装、升级、恢复、拆除和核心文件操作。
- 现有高级控制台功能回归清单逐项通过，不以“入口还在”代替真实验收。

### 24.10 软件内引导与新手可完成性

- 新用户分别从“新 VPS”“已有节点”“设备邀请”进入正确状态分支，不会看到无关步骤。
- 所有输入页包含目的、来源、格式、验证、影响、取消和恢复说明。
- OpenSSH 缺失、TUN/DNS 干扰、SSH 密码错误、Host Key 变化、Cloudflare 配置错误和磁盘不足均给出可执行修复动作。
- Cloudflare 向导严格一屏一项、一次验证；取消后进入安全一致状态。
- 中途关闭客户端、断网和安全停止后，重新打开可以从准确非秘密进度继续。
- 专家模式不隐藏真实风险，也不能绕过高风险确认和验收。
- 内置离线帮助中心在无网络时可以解释全部核心术语和流程。
- 无 VPS 经验测试者不看外部教程、不接受口头提示完成首装、交接单保存、网盘 CRUD、面板/隧道关闭和错误恢复。
- 测试者任何“这里输入什么、现在能否关闭、失败后去哪里”的阻塞都形成缺陷并修复后重测。

---

## 25. 发布完成定义

只有同时满足以下条件，才能把本次施工认定为完成：

1. Windows 外层是真实网盘前台，不是日志包装。
2. 普通账号可以登录并完成真实文件操作。
3. 内层完整保留当前高级图形运维控制台。
4. admin 入口门禁默认开启，并可在内层修改。
5. 高风险动作始终受二次认证保护。
6. 网盘不需要域名、证书、Cloudflare 或 Nginx。
7. VPS 网盘没有公网监听。
8. 本地和远端端口冲突能够有限、可解释地处理。
9. 上传、下载、续传、任务恢复和挂载通过验收。
10. 新设备邀请在真实绑定完成后才失效。
11. 每设备 SSH、VLESS 和网盘端点访问能力可以独立吊销；节点级普通网盘账号不随设备吊销。
12. 三种代理模式、六种互切和 `[0]` 全部通过。
13. `[1]` 仍是唯一安装入口。
14. “拆除施工和恢复基线”按节点状态只显示合法动作：代理存在时只能仅拆代理或整体恢复基线，代理已拆后才能单拆剩余网盘。
15. 完整拆除后可以从 `[1]` 重新安装。
16. 施工台账覆盖全部新增资源。
17. 失败分支不会继续复制秘密、开面板或删旧数据。
18. Windows 和 Android 中文界面完整可用。
19. 所有发布包通过隐私和供应链扫描。
20. 永久取消的五项功能没有按钮、隐藏入口或残留半成品。
21. 本机 admin 可以通过系统凭据库或加密恢复包可靠恢复。
22. 当前选择的普通网盘真实账号密码完整进入交接单并通过实际登录校验。
23. 普通网盘账号可以在外层凭旧密码和两次新密码安全改密。
24. 本机 admin 只能进入内层高级控制台主动改密。
25. 新电脑没有节点时外层如实显示“未挂载节点”并引导高级控制台。
26. 网盘是所有正常受管代理节点的强制组成部分，不能跳过。
27. 普通网盘账号可以在任一受信设备外层注册，并在该节点所有受信设备上访问同一空间。
28. 每台 VPS 最多 2 个普通网盘账号，加上 admin 最多 3 个隔离空间；初始登录页直接提供注册入口。
29. 三空间默认采用安全池三等分的保底额度，并可在不侵占其他空间和系统余量的前提下自适应拉高。
30. 普通账号注册只在 VPS 完整施工和远端实时验收均通过后开放，任何失败分支都不产生幽灵账号。
31. 切换当前网盘 VPS 必须返回目标节点登录页重新认证，旧节点账号和会话不能串入新节点。
32. `[1]` 能识别完整拆除、仅拆代理、拆除中断和人工缺件，并按台账恢复允许恢复的部分。
33. 管理面威胁模型落地：严格 SSH 模式不覆盖用户 key，未批准设备不能登录，厂商救援路径继续有效。
34. 节点级全局操作协调器阻止并发破坏性施工，崩溃接管和过期提交均通过验收。
35. 文件上传具备原子提交、校验、冲突副本、路径隔离、磁盘与 inode 安全停止和明确删除语义。
36. 首页准确显示数据是否有异机备份，并完成至少一次空 VPS 真实恢复演练。
37. 普通账号托管密文不能由 VPS 单独解密；登录限速、会话超时和退出全部设备有效。
38. 发布物具有内部 Build ID、签名清单、哈希、SBOM 和第三方许可；更新失败可回滚。
39. 本机卸载、设备退役、恢复材料保留和最后 controller 保护全部图形化可执行。
40. 日志结构化、脱敏、轮转，支持包默认无秘密，产品默认零遥测和零自动崩溃上传。
41. 支持平台矩阵、可用性验收和现有高级功能回归合同全部完成。
42. 软件内引导覆盖新 VPS、已有节点、设备邀请、Cloudflare、施工、凭据留存、网盘验收和错误恢复。
43. 没有 VPS 经验的测试者能够在不阅读外部教程、不接受口头提示的情况下完成首用验收。

---

## 26. 需求追踪表

| 用户要求 | 本文件落点 | 验收依据 |
|---|---|---|
| 外层是本地网盘客户端 | 第 5、19 节 | 普通账号登录后直接进入文件前台 |
| 外层可用普通账密 | 第 3.2、5.1 节 | 普通文件账号完成服务端真实认证 |
| admin 也能用账密直接登录外层 | 第 1.2、3.1、5.1 节 | `admin` 在本机验证后使用受信设备远端访问能力进入同一文件前台 |
| 本机 admin 账密必须可恢复 | 第 1.3、3.1、4.1、4.2 节 | 系统凭据库恢复与加密恢复包跨重装恢复均通过 |
| 普通网盘账密直接进入交接单 | 第 4.4 节 | 交接单显示真实 `DRIVE_USERNAME`/`DRIVE_PASSWORD` 并真登录成功 |
| 普通网盘账号可以在外层注册 | 第 3.2、5.1.2 节 | 受信设备完成注册、真登录、CRUD、托管和失败回滚 |
| 每台 VPS 最多两个普通账号 | 第 3.2、5.1.2、14 节 | 前两个注册成功，第三个原子拒绝且无残留；连同 admin 总空间数不超过 3 |
| 注册入口直接位于初始登录页 | 第 1.2、5.1.2 节 | 未登录状态即可看到注册入口；未绑定节点和账号满额时给出准确分支 |
| 三空间保底平均且可自适应拉高 | 第 14 节 | `SAFE_DRIVE_POOL` 三等分保底，扩容不侵占系统余量、其他空间或未注册槽位余量 |
| VPS 完整施工后才允许普通注册 | 第 5.1.2、13.3 节 | 仅三种 `*_WITH_DRIVE` 稳定状态和 `DRIVE_REGISTRATION_READY=1` 放行，故障注入后无幽灵账号 |
| 切换网盘 VPS 强制重新登录 | 第 5.4.1、10.2、11 节 | 切换后回目标节点登录页，旧会话清空，相同用户名也不会串空间 |
| 一个普通账号跨受信设备访问同一空间 | 第 3.2、5.1.2 节 | 设备 A 注册、设备 B 登录后 `DRIVE_SPACE_ID` 和文件一致 |
| 普通账号在外层凭旧密码改密 | 第 4.3.1 节 | 旧密码 + 两次新密码、真登录、交接单更新和失败回滚均通过 |
| admin 必须进入内层主动改密 | 第 1.3、6.1.1、19.1 节 | 外层无 admin 改密表单，内层事务更新和新密码复验通过 |
| 内层保留现在整套高级功能 | 第 1.2、6 节 | 当前高级导航和有效操作完整存在 |
| 进入高级功能默认验证 admin | 第 1.3、5.5 节 | 首次设置与默认门禁测试 |
| 内层可关闭门禁 | 第 1.3、6.1 节 | 修改开关需要 admin，设置只影响本机 |
| 新机器如实显示未挂载节点 | 第 5.1.1 节 | 无节点时不显示假文件/容量，并可进入高级控制台 |
| 网盘不要域名 | 第 1.4、8 节 | 无 DNS、证书、Cloudflare、Nginx 网盘配置 |
| 网盘不能抢公网端口 | 第 8.4、8.5、9 节 | 仅回环监听，冲突时迁移 |
| 网盘是受管节点强制组成 | 第 1.4、8.0、13.3 节 | 三种代理状态全部带健康网盘，网盘失败时 `[1]` 不交付 |
| `[1]` 是唯一安装入口 | 第 1.5、13 节 | 其他菜单不能 bootstrap |
| `[1]` 识别并恢复已拆除部分 | 第 7.2、8.0、13.2.1、16.2 节 | 四类拆除/缺件状态均生成资源级恢复计划；仅拆代理时所有网盘与身份对象保持不变 |
| 三种线路和互切 | 第 7、24.2 节 | 3 初装 + 6 转换 + 幂等 + `[0]` |
| 新设备邀请绑定成功才失效 | 第 12 节 | 中断续跑和消费状态测试 |
| 未批准设备不能 SSH 登录 | 第 3.3、12 节 | 端口响应但认证失败 |
| VNC 继续兜底 | 第 3.3、16.4 节 | 不锁本地 root 密码，最后处理 SSH |
| 能装也能拆 | 第 15、16、23 节 | 每个资源有安装、验证、回滚、拆除 |
| “拆除施工和恢复基线”长期保留 | 第 1.6、16 节 | 内层存在按节点状态动态显示的拆除入口 |
| 代理存在时不能单拆网盘 | 第 1.6、16.2 节 | 正常状态只显示仅拆代理/整体拆除 |
| 代理拆除后才显示网盘拆除 | 第 8.0、16.2 节 | 仅 `PROXY_REMOVED_DRIVE_RETAINED` 显示剩余网盘拆除 |
| 整体拆除直接恢复基线 | 第 16.4、16.7 节 | 完成后为 `BASELINE_UNMANAGED`，无 TNA 服务、文件卷和状态残留 |
| 不做五项候选增强 | 第 1.7 节 | UI、源码和发布包无残留入口 |
| 不内置真实隐私信息 | 第 21 节 | 源码与解包扫描通过 |
| 保护 VPS 管理入口而非伪造物理订阅锁 | 第 3.3、12、29.1 节 | 严格 SSH、按设备 key 吊销、救援路径和订阅可复制边界均如实验收 |
| 节点级全局施工锁 | 第 29.2 节 | 并发施工、租约超时、接管和过期 fencing token 测试通过 |
| 文件数据不能假完成或串空间 | 第 10、14、29.3 节 | 原子提交、冲突副本、路径隔离、磁盘/inode 和回收区测试通过 |
| 真实备份状态和空机恢复 | 第 17、29.4 节 | 未备份不冒绿、RPO 如实显示、空 VPS 恢复和哈希复核通过 |
| 凭据密文不能由 VPS 单独解密 | 第 4.4、29.5 节 | 服务器仅持密文无法还原；controller 解密、丢失和轮换分支通过 |
| 发布签名和内部构建可识别 | 第 1.1、21、29.6 节 | Build ID、签名清单、APK 签名、哈希、SBOM 和回滚一致 |
| 本机也能安全卸载和退役 | 第 29.7 节 | 四种本地分支、最后 controller 保护和进程/盘符清理通过 |
| 日志可支持但不泄密 | 第 18、29.8 节 | 操作 ID、错误码、脱敏轮转、支持包和零遥测默认通过 |
| 支持平台和旧功能均有合同 | 第 20、23、29.9、29.10 节 | 平台矩阵、可用性和全部高级功能回归清单通过 |
| 第一次使用者无需外部教程 | 第 29.11—29.15 节 | 无经验测试者独立完成首装、Cloudflare、交接、网盘和故障恢复 |

---

## 27. 施工状态登记

当前状态：**已于 2026-08-25 获得用户授权并开始分波施工；发布验收完成前不得宣称正式可交付。**

| 阶段 | 状态 | 说明 |
|---|---|---|
| A 台账与事务框架 | 进行中 | 已建立节点级租约、心跳、过期接管确认和 fencing token 骨架；施工台账待接入全部远端资源 |
| B 拆除施工和恢复基线基础 | 未开始 | 下一波建立新旧资源双识别和恢复事务 |
| C 本地身份根 | 进行中 | 已切换 TNA 新写入路径并加入 PNA copy-first 迁移；恢复包和 admin 根待实现 |
| D 设备准入 v2 | 进行中 | 本地 v2 身份/邀请格式已建立且保留 v1 只读兼容；远端批准态和 SSH key 绑定待实现 |
| E 无域名网盘 | 未开始 | 等待用户明确授权施工 |
| F Windows 双层客户端 | 未开始 | 等待用户明确授权施工 |
| G Windows 挂载 | 未开始 | 等待用户明确授权施工 |
| H Android 对齐 | 未开始 | 等待用户明确授权施工 |
| I 代理拓扑联合收敛 | 未开始 | 等待用户明确授权施工 |
| J 成熟产品封口 | 进行中 | 换行/清单基线、迁移防符号链接、全局协调器已先行；其余随各波收口 |
| K 软件内新手引导 | 未开始 | 各状态机和核心流程稳定后执行 |
| L 发布验收 | 未开始 | 波次 0：Go 单测、go vet 与现有 14 组 Shell/静态测试已全绿；完整矩阵待前置阶段完成 |

---

## 28. 技术参考

- copyparty 项目与权限模型：<https://github.com/9001/copyparty>
- copyparty rclone 文档：<https://github.com/9001/copyparty/blob/hovudstraum/docs/rclone.md>
- rclone mount 文档：<https://rclone.org/commands/rclone_mount/>
- WinFsp 许可证：<https://github.com/winfsp/winfsp/blob/master/License.txt>
- copyparty MIT 许可证：<https://github.com/9001/copyparty/blob/hovudstraum/LICENSE>

---

## 29. 成熟产品封口与软件内引导

本节不是候选增强功能，而是公开发布前必须完成的产品封口。不得以“功能已经能跑”为由跳过。

### 29.1 管理面安全目标与威胁边界

本产品的首要安全目标是阻止未批准设备登录和管理 VPS，而不是把标准代理订阅做成不可复制的物理设备锁。

- 每台受信设备使用独立 SSH key，VPS 按设备公钥和稳定 `DEVICE_ID` 授权。
- 首个 controller 的新 key 已经真登录验证，且厂商 VNC、串口或救援控制台可用后，才允许启用严格 SSH 模式。
- 严格模式关闭公网 SSH 密码和键盘交互认证；root 本地控制台密码继续保留用于厂商救援，不得被静默清除或锁死。
- TNA 只管理 `authorized_keys` 中带稳定资源 ID 的自身区块，不能覆盖用户原有 key；吊销设备只删除该设备的精确公钥和相关能力。
- Host Key 变化必须进入独立高风险确认流程，展示旧、新指纹和可能原因；不能自动接受，也不能让用户在普通输入框盲输 `yes`。
- 新设备在批准前不能 SSH 登录。邀请响应由新设备离线生成，现有 controller 批准并写入公钥，真登录和网盘端点能力均验证成功后邀请才消费。
- 邀请过期、证书验证和日志排序依赖时间时，远端必须预检时钟和 NTP 状态；明显漂移时先修正或指导，不得把时钟问题冒充凭据错误。
- IPv4、IPv6、仅 IPv4 VPS 和双栈系统的监听、防火墙、Host Key 与 DNS 行为必须分别验证；没有验证的地址族不能宣称受支持。
- 标准 `vless://` 或订阅材料一旦导出仍可复制。暂停或吊销其对应设备客户端后，所有副本同时失效；产品不得宣称它与物理硬件不可分离。
- 网盘传输由 SSH 隧道保护，但 VPS 文件卷默认不是端到端加密。界面和说明书必须明确：拥有 VPS root、宿主机磁盘或厂商后台控制权的人可能读取服务器上的文件；本版不得使用“零知识”或“端到端加密网盘”等误导表述。

### 29.2 节点级全局操作协调器

除账号注册自己的短事务锁外，每台节点还必须有覆盖所有破坏性操作的全局协调器。至少持久化：

```text
NODE_OPERATION_ID
NODE_ID
OWNER_DEVICE_ID
OPERATION_TYPE
STARTED_AT
LEASE_EXPIRES_AT
FENCING_TOKEN
CURRENT_STAGE
RECOVERY_STATE
LAST_HEARTBEAT
```

以下操作彼此互斥：

- `[1]` 安装、升级、拓扑转换和缺失组件恢复。
- 拆除施工和恢复基线。
- 配置恢复和 schema 迁移。
- VPS、面板、普通账号和 SSH key 轮换。
- 设备批准、吊销以及最后 controller 变更。
- 配额结构变更和账号空间删除。

规则：

- 只读诊断、状态查看和日志下载可以并行，但必须标明读取到的 `FENCING_TOKEN`。
- 活跃 controller 必须续租；客户端崩溃或网络断开后允许在租约超时并完成现场复核后接管。
- 旧持有者即使恢复网络，也不能使用过期 fencing token 提交配置。
- 接管前显示原操作、原设备、最后阶段和恢复计划，不能仅凭锁超时直接重跑完整脚本。
- Windows 同一用户默认单实例；第二实例可以转到现有窗口或只读打开，不能抢占隧道、盘符和施工输入。

### 29.3 文件一致性、冲突与存储压力

- 上传必须写入账号空间内的受管临时文件，完成长度和校验验证后执行 `fsync` 和原子重命名；没有提交的临时文件不能出现在正常文件列表。
- 程序崩溃、SSH 中断或 VPS 重启后，临时上传必须可继续或按保留策略清理，不能被误报为完整文件。
- 所有远端路径必须经过规范化并限制在对应 `DRIVE_SPACE_ID` 根目录内；拒绝 `..`、绝对路径、符号链接逃逸、设备名和跨平台非法文件名。
- 两台设备并发写入同一文件时禁止静默覆盖。默认保留先提交版本，并为后提交内容生成带设备和时间标识的冲突副本；用户确认后再合并或删除。
- 默认删除进入当前账号的受管回收区并保留 7 天，仍计入配额；“永久删除”和“清空回收区”必须明确确认。磁盘安全水位已触发时只允许用户主动确认的精确清理，不得后台随意删文件。
- 同时监控字节容量和 inode。达到预警线时提示；达到安全停止线时暂停新增写入和扩容，保留登录、读取、下载、删除和导出能力。
- 配额、实际占用、临时上传、回收区和备份占用必须使用同一统计口径，不能各页面给出不同数字。

### 29.4 数据备份承诺与真实恢复

- 每次施工、迁移、改密、账号删除和拆除前自动创建并验证配置备份。
- 网盘数据默认不虚假宣称已自动异机备份。未配置异机备份时，首页醒目标记“数据仅在当前 VPS，尚未建立异机备份”。
- 数据备份必须显示目标、范围、开始/完成时间、文件数、总字节、校验结果和最近成功时间。
- 用户看到的 RPO 是“自最近一次成功数据备份之后的变化可能丢失”；没有成功备份时显示“全部数据可能丢失”，不得显示含糊的绿色安全状态。
- 恢复页面必须说明预估恢复时间、所需本地/远端空间和将覆盖的对象；不承诺无法验证的 RTO。
- 发布验收至少进行一次：空 VPS 重建、导入配置备份、恢复网盘数据、核对账号/空间 ID、抽样哈希和真实登录 CRUD。
- 备份密钥、admin 恢复包和数据备份不得只放在同一台待恢复 VPS 上。

### 29.5 凭据托管、会话与登录防护

- 远端运行服务只保存密码哈希；需要完整交接的普通账号真实密码只能按第 4.4 节以 controller 可解密的密文托管。
- VPS 登录密码、面板真实密码和普通网盘密码的“可显示”状态必须来源于当前受信设备的安全仓库或可验证密文，不能从哈希推测；无法恢复时显示 `UNKNOWN_NOT_RECOVERABLE` 并提供显式轮换。
- 普通账号和 admin 登录失败必须有限速和递增等待；不得因为服务只监听回环就允许本机恶意程序无限暴力尝试。
- 普通网盘会话必须有空闲超时和绝对寿命；默认退出账号、改密、停用账号、删除账号、撤销当前设备时使相关会话和 Token 失效。
- 外层账号页面提供“退出当前设备”和“退出所有设备”；后者需要重新验证当前密码。
- 高级 admin 会话默认 15 分钟无操作后锁定，高风险动作仍单独二次认证。
- 日志、命令行、进程环境、崩溃转储、通知和截图不得包含完整密码、Token、私钥或解密后的托管内容。

### 29.6 发布信任链、内部构建身份和第三方合规

- 所有 v0.9.5 客户端的“关于”、诊断报告和发布清单必须显示平台对应的内部 Build ID、Git commit、构建时间和 schema 版本；不得只显示相同的公开版本号。
- 发布必须生成独立签名清单，覆盖 EXE、APK、Linux 工具包、依赖包和说明书的 SHA-256、大小和内部构建身份。更新器先验证签名，再验证单文件哈希。
- Android 使用固定发布签名并验证升级签名连续性；签名不匹配必须拒绝覆盖安装。
- Windows 有 Authenticode 证书时必须签名；没有证书时必须明确显示“未经过 Windows 发布者签名”，同时提供签名发布清单和校验工具，禁止伪装为已验证发布者。
- 更新必须由用户确认，不静默替换正在运行的客户端；保留上一个可启动构建和配置快照，健康验证失败时回滚。
- 每次发布生成 SBOM、第三方许可证清单和 `THIRD_PARTY_NOTICES`。MIT 只覆盖 TNA 自有代码，不能覆盖或抹去 copyparty、rclone、WinFsp、3x-ui、OpenSSH、Android SSH 依赖和模板素材各自的许可条件。

### 29.7 本地卸载、重置与设备退役

本地客户端必须提供图形化卸载前向导：

```text
[1] 仅卸载本机客户端，保留远端设备权限和可恢复材料
[2] 撤销当前设备的远端权限后卸载
[3] 导出并验证恢复包后，撤销当前设备并卸载
[4] 仅清理本机缓存、日志和非秘密历史
[0] 取消
```

- 如果当前设备是最后一个 controller，选项 `[2]`、`[3]` 必须先要求批准另一个 controller 或验证厂商救援路径，不能把用户锁在 VPS 外。
- 卸载前精确显示将删除和保留的 Credential Manager/Keystore 项、SSH key、恢复包、节点历史、日志、传输临时文件、隧道和盘符。
- 已挂载盘符、活动传输和 SSH 隧道必须先有序停止；失败时不能留下一个看似卸载完成但仍运行的后台进程。
- 卸载本机客户端不会自动拆除 VPS；“本机卸载”和“拆除施工和恢复基线”必须明确区分。

### 29.8 结构化日志、诊断与隐私

- 每个工作流使用统一 `NODE_OPERATION_ID` 和阶段码，本地、SSH 包装和远端脚本日志可以关联。
- 错误同时包含稳定错误码、用户可读说明、建议动作和可展开的技术详情；不能只显示 `rc=1` 或一段内部异常。
- 日志按字段脱敏，并设置单文件大小、总大小和保留期；默认保留不超过 7 天或 50 MiB，用户可以主动清理。
- 一键支持包默认包含 Build ID、非秘密节点状态、错误码、时间线、依赖版本和脱敏日志；默认排除密码、Token、私钥、真实文件内容和完整用户文件名。
- 默认零遥测、零广告、零自动崩溃上传。以后若增加任何外发诊断，必须逐次取得明确同意并先展示将发送的内容。

### 29.9 支持平台、可用性与兼容性

- 发布前必须根据真实测试结果冻结 Windows、Android、Linux 发行版、CPU 架构和最低系统版本支持表；未在表内的平台施工前即停止并解释，不能装到一半才失败。
- Windows 至少分别验证高 DPI、125%/150%/200% 缩放、中文输入法、键盘全流程、密码框、屏幕阅读器基础名称和高对比度状态。
- 状态不能只靠颜色表达；错误、警告、成功同时使用文字和图标。
- Android 必须验证前台隧道通知、后台限制、旋转/进程回收、文件选择器权限和大文件传输恢复。
- Linux 支持表必须说明 systemd、软件包管理器、防火墙后端、IPv6 和 CPU 架构要求。

### 29.10 现有功能回归合同

“完整保留高级控制台”必须落实为逐项功能清单，而不是一句总括。至少为下列现有功能登记前置条件、远端改动、成功证据、失败停止、回滚和自动化测试：

- OpenSSH 检测、一次性安装和版本验证。
- 面板隧道打开、关闭和白屏诊断。
- VPS 密码、3x-ui 账号密码和 SSH key 轮换。
- 当前凭据交接单。
- 自动体检、安全修复和紧急诊断报告。
- 前台模板、Nginx 回落和 15 套模板选择。
- v2rayN 导入与订阅验真。
- 本地 10808 环境变量配置、查看和撤销。
- 配置备份、完整灾备、备份清理和图形化恢复。
- 节点历史、key 备份/恢复、设备准入和流量中心。
- 服务商 API 凭据的临时输入、Credential Manager/Keystore 保存、查看状态和删除。
- 拆除施工和恢复基线。

任何一项没有明确验收用例，都不能仅凭旧版本曾经工作就宣称已保留。

### 29.11 软件内引导是产品主流程

默认体验必须让第一次使用 VPS、域名、DNS 和 SSH 的用户不依赖外部教程完成主要流程。说明书是补充，不能代替软件内引导。

- 默认启用“引导模式”；可以切换“专家模式”，但安全确认、真实验收和数据删除保护不能跳过。
- 引导必须由当前节点和本机的真实状态驱动，不是每次都从第一页开始的静态幻灯片。
- 一屏只处理一个目标或决定；禁止连续抛出多个终端式问题，也禁止在日志区等待用户猜测该输入什么。
- 所有输入和确认使用原生图形控件。命令行输出仅作为可展开技术详情，不作为唯一操作界面。
- 非秘密进度必须持久化。程序关闭、网络中断或安全停止后，重新打开应从准确的可恢复步骤继续。

### 29.12 首次使用的标准引导

首次启动至少按以下顺序引导：

1. 检查操作系统、OpenSSH、依赖、网络、本机代理和 TUN/DNS 干扰。
2. 选择“施工新 VPS”“接管已有节点”或“响应 controller 邀请加入设备”。
3. 填写 VPS 地址、SSH 用户和端口，并说明这些信息在服务商控制台的获取位置。
4. 验证 SSH Host Key、登录方式、设备 key 和厂商 VNC/救援入口；解释绑定后会发生什么。
5. 用三张卡片选择仅灰云、仅橙云或双路，逐项说明速度、可用性、配置复杂度和 Cloudflare 要求。
6. 需要 Cloudflare 时，每次只指导一个设置：用户完成后点击“验证并继续”，失败时显示具体修正动作，输入或点击取消则安全回滚。
7. 执行 `[1]`，图形化显示当前阶段、已经完成项、正在修改的对象、可安全取消点和失败恢复入口。
8. 完成后展示必须保存的 VPS、面板、网盘、设备、订阅和恢复凭据，说明保存位置并引导写入密码管理器。
9. 引导注册或登录普通网盘账号，上传一个测试文件、下载校验，并按平台选择是否挂载。
10. 执行最终一键验收，分别报告“现在可用”“尚未完成”“需要留存”“建议下一步”，不能只显示一个绿色完成文字。

已有节点不得被迫重复全新教程。程序应先识别：

```text
NO_BOUND_NODE
BASELINE_UNMANAGED
三种完整 *_WITH_DRIVE
PROXY_REMOVED_DRIVE_RETAINED
损坏或恢复中状态
等待设备邀请批准
```

然后只展示与当前状态有关的下一步。

### 29.13 每一步的固定说明模板

所有会要求用户输入、确认或等待的步骤必须展示：

```text
这一步要做什么
为什么需要
应该输入什么
去哪里找到
正确格式或脱敏示例
程序将如何验证
会修改、保留或删除什么
失败后怎么办
如何取消和恢复
```

- 密码输入框必须明确密码属于 VPS、面板、普通网盘、本机 admin 还是服务商 API，并说明是否保存、保存到哪里、如何删除。
- 错误页面先说人能执行的修正动作，再允许展开稳定错误码、退出码和原始脱敏日志。
- 危险操作必须在确认页列出影响范围、备份状态和救援路径。
- 长任务持续显示阶段、最近活动和可安全停止状态，不能无限停留在“处理中”。

### 29.14 内置帮助中心

Windows 和 Android 都必须内置可离线访问、可搜索的帮助中心，使用一致术语解释：

- VPS、IP、SSH 用户、端口、Host Key、SSH key、VNC/救援控制台。
- 域名、子域名、A/AAAA/CNAME、DNS 传播、灰云、橙云、SSL/TLS 和 Origin Rule。
- 仅灰云、仅橙云、双路的优缺点和切换影响。
- 3x-ui 面板、订阅、设备准入、controller、普通网盘账号和本机 admin 的区别。
- 网盘只走 SSH 回环隧道、文件在 VPS 上不是端到端加密、备份状态和数据删除风险。
- `[1]`、诊断、恢复、拆除施工和恢复基线各自负责什么。

帮助页可以链接在线更新说明，但核心流程不能因断网或网页失效而无法解释。

### 29.15 新手可完成性验收

发布前必须让没有使用过 VPS、SSH、Cloudflare 或 3x-ui 的测试者在不阅读外部教程、不接受开发者口头提示的情况下完成：

- 识别并填写一台测试 VPS。
- 选择一种线路模式并完成所需 DNS/Cloudflare 步骤。
- 完成施工、保存交接单并理解哪些秘密必须保留。
- 注册普通网盘账号，上传、下载并验证文件。
- 打开和关闭面板/网盘隧道。
- 在一个故意制造的 DNS/TUN、密码或 Host Key 错误中找到正确恢复动作。
- 安全退出，随后重新登录并找到帮助、诊断、备份和拆除入口。

验收人员如果必须询问“这里输入什么”“现在能不能关”“失败后该点哪里”，即判定对应引导未完成，不能把问题归因于用户不会用。

---

## 30. 变更记录

### 成熟产品封口与软件内引导补充

- 明确核心安全目标是保护 VPS 管理入口，设备独立 SSH key、严格模式和厂商救援形成闭环；标准订阅不伪装成物理硬件锁。
- 增加节点级全局操作协调器、租约、fencing token 和并发施工接管规则。
- 增加文件原子提交、并发冲突、路径隔离、回收区、磁盘与 inode 压力处理。
- 明确未配置异机备份时必须如实显示，并加入空 VPS 数据恢复演练。
- 普通账号真实凭据远端托管改为 controller 可解密密文，VPS 单独不能还原。
- 增加会话限速、超时、退出所有设备、发布签名、内部 Build ID、SBOM 和第三方合规要求。
- 增加本地卸载/设备退役、日志脱敏轮转、支持包、零遥测、平台矩阵和旧功能回归合同。
- 软件内引导升级为主流程：状态驱动、一屏一项、可恢复进度、离线帮助和无经验测试者独立完成验收。

### 设计基线初稿

- 固定对外版本为 v0.9.5。
- 明确外层普通网盘、内层完整高级控制台的双层结构。
- 明确 admin 门禁默认开启、可在内层关闭。
- 网盘固定为无域名、SSH 隧道、VPS 回环监听。
- 保留三种代理拓扑和六种互切。
- 将设备准入升级为真实绑定成功后才消费邀请的可恢复状态机。
- 将施工台账和“拆除施工和恢复基线”设为新增功能的前置条件。
- 永久取消五项候选增强功能。

### 外层 admin 登录补充

- 明确外层登录同时接受普通网盘账号和本机 `admin` 账号密码。
- admin 密码只在本机验证，不复用为远端 copyparty、SSH、VPS 或面板密码。
- admin 登录外层后使用当前受信设备的远端访问能力访问共享管理员空间。
- admin 外层会话进入高级控制台时，门禁开启状态下仍执行重新认证。

### admin 恢复与普通账号交接补充

- 本机 admin 不再只保存不可逆校验值；Windows/Android 系统凭据仓中保存受系统保护的可恢复记录。
- 首次创建和每次修改 admin 密码时生成新的 `.tna` 加密恢复包与恢复码，并显示准确保存路径。
- 本机 admin 恢复材料不进入远端 VPS 交接单。
- 当前选择的普通网盘真实用户名和密码直接进入完整交接单。
- 远端运行配置仍只保存网盘密码哈希；真实普通账号凭据只以 controller 可解密的 root-only 密文托管，供受控交接，VPS 单独不能解密。

### 普通账号与 admin 改密边界补充

- 普通网盘账号可以在外层输入旧密码、新密码和再次确认新密码，修改自身密码。
- 普通账号改密必须经过新会话真登录、最小读写验证、交接单更新和失败回滚。
- 本机 admin 的恢复入口只恢复已有凭据，不能在外层设置新密码。
- 本机 admin 的主动改密入口只存在于内层高级运维控制台的“安全与凭据”。

### 外层普通账号注册与跨设备空间补充

- 外层登录页增加“注册普通网盘账号”。
- 注册请求必须来自 active 受信设备的 SSH 回环隧道，但注册出的普通账号不绑定该设备。
- 普通账号是节点/空间级身份，拥有稳定 `DRIVE_ACCOUNT_ID` 和 `DRIVE_SPACE_ID`。
- 同一普通账密可以在该节点所有受信设备上访问同一空间。
- 新设备准入只交付网盘登录/注册端点访问能力，不自动创建新的普通账号。
- 吊销设备不删除普通账号；账号禁用、改密或删除才是影响所有登录设备的账号级操作。

### 强制网盘与拆除状态补充

- 新电脑没有绑定节点时，外层显示“未挂载节点”，主引导进入高级运维控制台，不显示假网盘或假容量。
- 网盘从可选组件升级为所有正常受管代理节点的强制组成部分和流量特征伪装基础。
- 菜单 `[1]` 不提供跳过网盘的选择；网盘未通过验收时整个施工不交付成功。
- 代理存在时，“拆除施工和恢复基线”只显示“仅拆代理、保留网盘”和“整体拆除、恢复基线”。
- 只有代理已经拆除并识别为 `PROXY_REMOVED_DRIVE_RETAINED` 时，才显示拆除剩余网盘的入口。
- 整体拆除直接收敛到 `BASELINE_UNMANAGED`，删除 TNA 网盘和文件卷前必须完成数据导出验证或取得明确永久删除确认。

---

## 35. 已确认对话决策与实施波次（2026-08-25）

本节把最后两轮产品讨论中已经明确的约束写成可测试基线。它们不是建议项，不得在实现时自行弱化。

### 35.1 新手引导是功能，不是文档补丁

- 第一次使用者必须能仅依靠软件内文案完成 VPS 准备、Host Key 核对、线路选择、Cloudflare 配置、真机验收、凭据保存、设备准入和恢复。
- 每个人工步骤必须说明“去哪里、看什么、做什么、成功长什么样、失败怎么办”。
- Cloudflare 橙云步骤按 Proxied、Universal SSL、Full (strict)、直接使用 Edge `:8443`、Cache Bypass、无拦截规则逐项暂停；免费计划不创建 Origin Rule，避免把请求误送到 Reality `:443`；每项确认后继续，`q` 安全取消。
- 不允许空回车跳过新机线路选择；`0` 只在已有完整受管拓扑上显示。
- 不允许假设用户理解灰云/橙云。界面必须同时解释延迟、源站暴露、Cloudflare 依赖和双路维护成本。

### 35.2 唯一施工入口

- `[1]` 是上传/升级远端包、安装强制网盘、恢复缺失组件和灰云/橙云/双路互切的唯一入口。
- `[22]` 只读显示拓扑、域名角色、证书、Cloudflare 探针和下一步指引，不得独立施工。
- 同构建跳过重复安装；旧构建备份后升级；远端更新构建拒绝降级；远端包缺失但业务存在时只恢复管理层。
- 部分拆除状态必须由 `[1]` 根据事务回执和现场探针恢复，禁止把每个菜单动作都隐式变成重装入口。

### 35.3 安全设备准入的简明语义

- 严格模式启用后，未获准设备没有可用 SSH 登录凭据，公网密码认证按策略关闭；厂商 VNC/Console 始终保留为救援路径。
- 新设备不需要预先登录 VPS 或进入同一 VPS 菜单。首页提供无需认证的“响应邀请”入口，只生成本机身份、SSH 公钥和签名响应。
- controller 批准响应后设备进入待首次验证；邀请只有在新设备用独享 key 完成首次真实登录并写入 active 状态后才失效。
- 审批、网络中断或客户端退出都不得提前消费邀请；过期、撤销或成功绑定才终止邀请。
- 订阅和 SSH 管理权彼此独立；每设备节点可暂停/吊销，但订阅仍是可复制秘密，不宣传为硬件不可复制身份。

### 35.4 双层网盘与账号

- 启动外层接受普通网盘账号和本机 admin；admin 也可以直接访问外层管理员空间。
- 高级控制台默认要求 admin 二次验证，可在内层关闭；本机 admin 只能在内层主动改密。
- 普通账号可在外层用旧密码并两次输入新密码改密；每 VPS 最多两个普通账号。
- 普通账号只在远端完整施工、网盘健康且当前设备 active 后允许注册，禁止幽灵账号。
- 普通账号是 VPS/空间级身份，可以在该 VPS 的所有受信设备登录同一空间，不绑定注册设备。
- 切换网盘 VPS 时强制注销，回到登录页并要求目标 VPS 的网盘账密，避免串号。
- 新设备或新电脑没有已施工节点时，外层必须显示“未挂载节点”并引导进入高级控制台或设备邀请，不能展示假容量、假文件或可注册账号。
- 网盘无域名、无公网端口，随机使用 `39000—39999` 回环端口并经 SSH 隧道访问。

### 35.5 拆除施工和恢复基线

- 功能名称固定为“拆除施工和恢复基线”。
- 代理存在时只显示“仅拆代理、保留强制网盘”和“整体拆除、恢复基线”。
- 只有代理已拆除时才显示“拆除剩余网盘”。
- 整体拆除恢复可信原生基线；旧节点缺少可信快照时标记 `LEGACY_UNCERTAIN`，不得声称完全恢复。
- 每次危险拆除必须先生成、下载并校验救援包；数据卷永久删除必须再次精确确认。
- 重装、恢复和拆除后回补仍只进入 `[1]`。

### 35.6 实施波次与发布门禁

| 波次 | 范围 | 完成条件 |
|---:|---|---|
| 0 | 基线、隐私、命名 | 无真实身份数据；旧状态只读迁移 |
| 1 | TextNodeAssistant 品牌 | Windows/Android/工具包/文档统一，兼容身份不破坏升级 |
| 2 | admin、设备、安全 | 恢复包、邀请首次绑定消费、最后 controller 保护 |
| 3 | 三模式拓扑 | 灰/橙/双路安装与互切只能走 `[1]` |
| 4 | 强制网盘、拆除 | 动态回环端口、账号边界、仅拆代理/整体拆除/恢复 |
| 5 | Windows GUI | 外层网盘、内层完整图形控制台、输入和停止无死循环 |
| 6 | Android | 原生双层界面、Keystore、邀请、网盘、同协议 |
| 7 | 发布工程 | 全平台构建、测试、SBOM、哈希、隐私扫描、文档 |
| 8 | 真实 VPS | 备份→基线→三模式→互切→设备→网盘→拆除→恢复全部通过 |

任何波次未通过时不得把“打印成功”或“部分可用”当作发布完成。真实 VPS 最终矩阵必须记录事务 ID、远端状态、功能探针和回滚结果，但报告中必须脱敏真实 IP、域名、邮箱与凭据。
