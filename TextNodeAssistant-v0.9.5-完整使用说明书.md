# TextNodeAssistant v0.9.5 完整使用说明书

## 1. 版本定位

本手册适用于“基于 v0.9.0 重建的精简重置版 v0.9.5”。它不是旧实验版 v0.9.5 的继续堆叠。

本版没有设备准入、controller、邀请、租约、网盘、Copyparty、普通网盘账户或本机 admin。界面中也不应出现这些功能的按钮、占位卡片或成功判定。节点长期 SSH key 仍然存在，但它只负责登录指定 VPS，不代表设备身份或准入资格。

如果旧文档提到“受信设备”“controller”“创建邀请”“私人文件空间”或“本机 admin”，该内容均不适用于本重置版。

## 2. 中英术语对照

| 中文界面 | English | 含义 |
|---|---|---|
| 保持现有线路 | Keep current routes | 已有节点不切换线路拓扑 |
| 灰云 / 直连 | Gray / direct | DNS only 域名直指 VPS，使用 Reality |
| 橙云 / CDN | Orange / CDN | Cloudflare Proxied 域名，使用 TLS + XHTTP `8443` |
| 双路 | Dual | 灰云 Reality 与橙云 XHTTP 同时存在 |
| 临时密码 | Temporary password | 只用于本项操作，不形成长期绑定 |
| 节点长期 key | Managed node key | 按主机和 SSH 用户保存的长期登录 key |
| 真机验货 | Real client validation | 把临时链接导入真实客户端并实际访问互联网 |
| 交接单 | Credential handoff | 经标记与字段完整性校验后的真实凭据摘要 |
| 失败关闭 | Fail-closed | 一处失败即停止所有后续动作 |
| 恢复基线 | Restore baseline | 拆除本工具管理的施工并保留 SSH 救援通道 |

## 3. 支持环境

### 3.1 客户端

- Windows 10/11 x64、x86、ARM64；
- Android 7.0 及以上的正式 universal APK；
- Windows 使用 OpenSSH；Android 使用应用内 SSH/SCP。

Windows 启动时会验证 `ssh`、`scp`、`ssh-keygen` 和 `ssh-keyscan` 是否来自同一套可用 OpenSSH。缺失时只尝试一次安装并复核；失败会明确停下，不会进入安装死循环。

### 3.2 VPS

- Ubuntu 或 Debian；
- 独立公网 IPv4；
- root，或具有可用 sudo 的普通账户；
- SSH 可达，服务商后台有 Console/VNC/救援入口；
- 具备足够的 CPU、内存和磁盘。建议至少 1 vCPU、1 GB 内存、10 GB 磁盘。

### 3.3 域名

- 灰云模式：DNS only 主机名必须解析到该 VPS；
- 橙云模式：Cloudflare Proxied 主机名不能在公网 DNS 直接泄露源站 IPv4；
- 双路：必须准备两个不同主机名，一个灰云、一个橙云；
- 域名和证书邮箱没有默认值，每次菜单 `[1]` 施工时本人手填。

## 4. 本地数据与隐私

### 4.1 会保存什么

- 可选的 VPS 历史：主机、SSH 用户、端口、标签和时间；
- 按 VPS + SSH 用户隔离的长期 key 与其可恢复备份；
- 已确认的 SSH Host Key；
- 不含主机、域名、邮箱和秘密的行为偏好，例如上次选择的性能档或是否成功后开面板；
- 经本人同意后保存的服务商 API 密钥：Windows 进入 Credential Manager，Android 进入 Keystore 加密仓。

### 4.2 不会保存什么

- VPS 登录密码；
- 3x-ui 明文密码到历史或普通日志；
- 域名、证书邮箱到通用偏好文件；
- API Key、Token 或私钥到发行 EXE/APK；
- 施工计划中的路线身份到 JSON 设置。

邮箱只在施工预览中显示遮罩形式。真实域名必须显示，便于人在真正修改前核对；该预览不应自动落入设置或长期日志。

## 5. SSH 登录模型

每一项远端操作都会重新选择登录方式和目标，不会沿用上一台 VPS 形成隐式锁定。

### 5.1 临时密码

适用于新机、临时维护或不希望在当前设备留下长期 key 的场景。

1. 输入主机、SSH 用户和端口；
2. 核对服务器公开 Host Key 指纹；
3. 由遮罩输入把密码交给 SSH；
4. 如需一次性 key，它只服务本次动作；
5. 动作结束撤销远端临时公钥并清理本机临时文件。

密码不进入命令行参数、历史、普通日志或剪贴板。

### 5.2 节点长期 key

适合自己的固定电脑/手机和长期维护的 VPS。

- key 按 `主机 + SSH 用户` 独立查找；
- 没有 key 时先用密码完成一次真实认证，再明确询问是否绑定；
- 轮换时先生成并安装新公钥，再用独立连接实测；
- 只有新 key 确认真能登录后，才处理旧公钥；
- 若旧公钥未能自动移除，保留双钥状态并明确提示，不会假装完成。

菜单 `[K]` 可以查看、归档、恢复 key，或把所有 key 转入备份态并空出绑定位置。归档不等于远端撤销；若要让某公钥失效，应使用明确的远端解绑流程。

### 5.3 Host Key

Host Key 是 VPS 的服务器身份，不是客户端私钥。首次连接需核对 SHA-256 指纹。服务器重装后指纹变化，必须先从服务商控制台确认，再替换本地固定记录。程序不应使用无条件信任来绕过验证。

## 6. 主菜单

| 编号 | 功能 | 是否连接 VPS | 是否可安装工具包 |
|---:|---|---:|---:|
| 1 | 安装 / 升级 / 自适应优化 | 是 | **是，唯一入口** |
| 2 | 打开 3x-ui 面板 | 是 | 否 |
| 3 | 自动体检与排障 | 是 | 否 |
| 4 | 安全自动修复 | 是 | 否 |
| 5 | 随机化 VPS 登录密码 | 是 | 否 |
| 6 | 随机化 3x-ui 账号密码 | 是 | 否 |
| 7 | 显示并复制当前凭据交接单 | 是 | 否 |
| 8 | 切换 15 套伪装站并优化 Nginx | 是 | 否 |
| 9 | 完整灾备 | 是 | 否 |
| 10 | 生成并下载紧急诊断报告 | 是 | 否 |
| 11 | 绑定 / 重新生成 SSH key | 是 | 否 |
| 12 | 清空本应用写入的秘密剪贴板 | 否 | 否 |
| 13 | 卸载远端内嵌包 | 是 | 否，只卸载 |
| 14 | 本地 10808 代理环境变量 | 否 | 否 |
| 15 | 整理远端备份 | 是 | 否 |
| 16 | 自适应性能档位 | 是 | 否 |
| 17 | SSH/vnStat 流量估算 | 是 | 否 |
| 18 | 全量拆除施工并恢复基线 | 是 | 否，只拆除 |
| T | 服务商流量中心 | 本地/API | 否 |
| K | 管理已绑定 key | 本地/可选远端 | 否 |
| H | 管理 VPS 历史 | 否 | 否 |
| C | 清空当前选择和隧道 | 否 | 否 |

菜单 `[13]` 之后若要恢复远端管理程序，只能重新运行 `[1]`。其他菜单检测到工具包缺失时应报明白，而不是偷偷上传。

## 7. 菜单 `[1]`：唯一施工入口

### 7.1 只读识别

程序先读取：

- 远端工具包版本、内部 revision、build ID 与关键文件完整性；
- x-ui 服务、程序或数据库是否存在；
- 现有路线状态是否属于受管灰云、橙云或双路。

识别阶段不得修改系统。只有出现完整计划并输入 `APPLY` 后，才允许上传或变更。

### 7.2 安装与版本判定

| 远端状态 | 菜单 `[1]` 行为 |
|---|---|
| 没有工具包 | 安装当前内嵌包 |
| 版本/构建完全相同 | 禁止重复上传和 bootstrap |
| 同版本但内部 revision 较旧 | 受控升级，不以“同为 v0.9.5”为由误跳过 |
| 可见版本较旧 | 先备份，再升级 |
| 远端构建较新 | 拒绝降级，要求换更新客户端 |
| 同构建但关键文件不完整 | 拒绝循环覆盖；按提示先 `[13]` 卸载，再回 `[1]` |

### 7.3 线路选择

必须输入一个明确数字，空回车无效。

#### `[0]` 保持现有线路 / Keep

仅识别到已有节点时显示。不切换证书、订阅、Reality/CDN 拓扑或伪装站，只更新需要的工具和显式选择的维护项。新机不能用 `[0]`，避免制造“看似成功但没有线路”的空节点。

#### `[1]` 仅灰云 / Gray

- 要求灰云域名为 DNS only，并解析到 VPS；
- 本人输入灰云域名和证书邮箱；
- 使用 Nginx、证书、Reality；
- 新建或危险修改 Reality 时，先在 `24443` 建 shadow 验货；
- 真实客户端可用后才提升到正式 `443`。

优点是链路短、吞吐和延迟通常更好；缺点是公开 DNS 会指向源站，且直连路径可能受某些网络影响。

#### `[2]` 仅橙云 / Orange

- 要求橙云域名开启 Cloudflare Proxied；
- 本人输入橙云域名和证书邮箱；
- 客户端连接橙云域名的 TLS/XHTTP `8443`；
- 源站 `8443` 只允许 Cloudflare 地址段，Windows 还会验证源站不能被普通公网直接拨通；
- 边缘响应必须带 Cloudflare 与受管源站证明；
- XHTTP 链接必须是所选橙云主机名、端口 `8443`、TLS、有效路径与客户端身份；
- 真机验货并精确输入 `REAL BROWSE OK` 后才提交；仅橙云最终会移除本工具管理的 Reality 路线。

优点是隐藏源站并可绕开部分直连路径故障；缺点是依赖 Cloudflare，配置和排障链更长。

#### `[3]` 双路 / Dual

- 准备一个灰云主机名和一个不同的橙云主机名；
- 分别手填两套域名与证书邮箱；
- 灰云 Reality 与橙云 XHTTP 都要完成验证；
- 任一条不通过，不把整体状态写成已完成；
- 成功后可在客户端保留两个独立节点用于手动切换。

双路维护项最多，但不会要求设备准入或 controller。

### 7.4 伪装模板

15 套模板全部内嵌，不依赖第三方字体、图片、统计脚本或远程 JavaScript。

| 输入 | 行为 |
|---|---|
| `0` | 保留当前模板，仅已有节点显示 |
| `R` | 真随机，并尽量避开当前模板 |
| `A` | 按域名稳定选择，同域名重复施工倾向保持一致 |
| `1—15` | 精确指定编号 |

空回车不会代替选择。模板名称会在界面完整列出。

### 7.5 性能档位

| 输入 | 模式 | 说明 |
|---:|---|---|
| `0` | Preserve / 保留 | 已有节点零修改，新机不可用 |
| `1` | Auto / 自动 | 按 CPU 与内存选择 |
| `2` | Low / 低配 | 节省内存，限制激进并发 |
| `3` | Standard / 标准 | 通用均衡 |
| `4` | High / 高配 | 使用更多 CPU/内存换取吞吐 |

性能变更有独立备份和回滚路径。高配不代表越高越好；小内存 VPS 强行使用可能造成服务被系统回收。

### 7.6 WARP 策略

- `[0] Preserve`：保持当前状态；未安装就不装，已安装也不改托管路由；
- `[1] Ensure on`：幂等安装/修复本地 WARP 代理与本工具管理的路由。

没有“空回车默认开启”。固定 WARP 回环端口为 `40000`，不对公网开放。

当前平台约束有一处明确差异：Android 对全新节点只接受 `[1] Ensure on`，已有节点才显示 `[0] Preserve`；Windows 会显示两项并要求明确选择。不要按另一平台的截图猜测本机可选项。

### 7.7 备份与收尾

- 已有节点变更前备份强制开启，不能取消；
- 可选择成功后整理旧备份，只保留一份新建且验证通过的当前配置备份；
- 可选择成功后打开面板；
- 以上两个收尾动作只有在核心施工、线路收敛和交接单都成功后才会执行。

### 7.8 完整预览与 `APPLY`

预览至少包括：线路、域名、遮罩邮箱、模板、性能、WARP、备份、收尾选项和固定端口组。

只有精确输入大写：

```text
APPLY
```

才会开始上传。输入其他内容、关闭输入或取消，必须在上传前结束。

### 7.9 单次输入文件

域名和邮箱不会拼进共享包。确认后，客户端为本次施工创建随机文件名的单次输入：

- 仅 root 可读；
- 权限 `0600`；
- 不允许符号链接替换；
- 文件名不可预测；
- 核心步骤与 CDN 步骤分别使用、分别清理；
- 失败、退出或回滚路径也会尝试删除。

不要把这类运行态文件复制到公开 issue、聊天或仓库。

### 7.10 真机验货

结构化诊断、TCP 端口可达和面板显示“启用”都不能替代真实流量。

- Reality：导入打印出的 `24443` shadow 链接，用真实客户端访问网页；确认可用后才提升到 `443`；
- Orange/Dual：导入橙云域名 `8443` 的 TLS/XHTTP 链接，真实浏览后按提示输入精确确认词；
- 不通时选择拒绝，不要虚假确认；程序应回滚暂存拓扑并保留原可用线路。

## 8. 其他菜单详解

### `[2]` 打开 3x-ui

读取结构化面板端口与路径，在本机建立 `127.0.0.1` 随机端口到远端回环面板的 SSH 隧道。面板不暴露公网。元数据为空、端口越界、路径危险或 SSH 隧道退出时都拒绝打开浏览器。

### `[3]` 自动体检

检查 SSH、x-ui、Nginx、证书、端口、UFW、WARP、订阅和受管路线状态。诊断不等于真机验货；客户端仍需真实连接测试。

### `[4]` 安全修复

先备份，再修复程序能够确定的问题。不能自动判断的域名、第三方反代、手写 Nginx 或未知端口占用会停下并报告，不强行覆盖。

### `[5]` VPS 密码轮换

生成真实高强度密码、更新系统账号、显示并复制。保存后及时清空剪贴板。长期 key 可让日常登录不依赖记忆密码，但仍应把密码保存在密码管理器和服务商救援记录中。

### `[6]` 3x-ui 身份轮换

生成新的面板用户名和密码，重启/验证后输出交接单。会注销旧会话；原有 2FA 可能受到影响。若真实 localhost 登录验证失败，不宣告成功。

### `[7]` 交接单

只接受带完整开始/结束标记、运行标记和至少一个有效凭据字段的输出。`Connection closed`、空文本或错误摘要不会被复制成“凭据”。历史哈希不能还原成明文密码；需要明文时使用显式轮换。

### `[8]` 伪装站 / Nginx

用于已有受管域名的模板切换和 Nginx 优化。检测到自定义网站时默认保留，避免覆盖用户内容。线路拓扑变更仍必须回到 `[1]`。

### `[9]` 完整灾备

包含程序、面板数据库、证书、WARP 身份和配置，体积较大且高度敏感。备份必须 root-only，下载后离线保存。

### `[10]` 紧急报告

生成并下载诊断报告。程序会尽量脱敏，但发送给第三方前仍需人工检查域名、地址、用户名、路径和日志片段。

### `[11]` 绑定 / 轮换 key

先验证新 key，再处理旧 key。失败时保留旧钥匙。不要手工删除唯一可用 key 后再测试。

### `[12]` 清空剪贴板

清理本应用可能写入的秘密。它不能清除密码管理器或其他应用已经读取的内容。

### `[13]` 卸载远端内嵌包

输入精确确认词后，只删除本工具包、维护命令和上传残留。保留 x-ui/Xray、Nginx、WARP、节点配置、凭据、证书和备份。卸载后节点可继续工作，但菜单管理脚本不存在；恢复只能用 `[1]`。

### `[14]` 本地 10808

此项不登录 VPS，也不自动证明 `127.0.0.1:10808` 上的程序可信。Windows 可配置、撤销或查看当前用户的 `HTTP_PROXY`、`HTTPS_PROXY` 与 `NO_PROXY`。Android 的 `[14]` 是独立的应用内开关：只让本应用的服务商 API 请求经 `10808`，不接管 SSH，也不修改 Android 系统 VPN/代理。

### `[15]` 整理远端备份

先创建并验证一份当前配置备份，再精确清理本工具已知的旧备份、前置副本和旧当前配置包。不会删除实时配置、证书、凭据或服务商快照。

### `[16]` 性能档

支持检测、查看、应用 low/standard/high/auto 和回滚。线路与端口不在该菜单更换。

### `[17]` SSH/vnStat 流量

显示服务器网卡视角的估算，并按 70/85/95% 提示。它可能包括服务商不计费流量，也可能无法看到宿主机层开销，因此不能替代厂商账单。

### `[18]` 全量拆除与恢复基线

高风险流程：

1. 读取拆除计划；
2. 明确输入精确确认词；
3. 在远端创建完整救援包；
4. 下载到本机并通过 SHA-256 校验；
5. 才移除本工具管理的节点、网站、证书、WARP、性能、流量组件、远端工具和备份；
6. 独立复核端口与服务；
7. 保留 SSH 配置、当前登录 key、22 端口和共享系统基础包。

救援包下载或校验失败时禁止拆除。

### `[T]` 服务商流量中心

KiwiVM 使用 VEID 与 Private API Key；其他服务商只有提供明确只读 HTTPS API 时才可接入。网站登录密码不属于 API 凭据。秘密仅在本人确认后进入平台安全仓，不进入 URL 和普通日志。

### `[H]` 历史目标

只管理主机、用户、端口和标签。删除历史不删除 SSH key；删除 key 也不应暗中删除历史。

## 9. Cloudflare 配置要点

### 9.1 灰云

- A 记录指向 VPS；
- Proxy status 为 DNS only；
- 公网 80 用于 ACME HTTP-01，443 用于正式 Reality；
- 不要把灰云域名改成橙云后继续使用原 Reality 订阅。

### 9.2 橙云

- A 记录内容为源站 IPv4，Proxy status 为 Proxied；
- SSL/TLS 使用 Full (strict)，Universal SSL 可用；
- 客户端使用橙云域名的 `8443`，而不是面板内部随机入站端口；
- 为该主机名设置不缓存动态 XHTTP 路径；不要叠加 Access、Turnstile、质询、重定向或 Worker；
- 免费计划不需要把所有 443 请求用 Origin Rule 改写到 8443；全请求改写会破坏 HTTP-01。若使用限定规则，HTTP `/.well-known/acme-challenge/` 必须仍能到源站 80；
- 源站 8443 由工具暂存并限制为 Cloudflare CIDR，普通公网直拨应失败。

### 9.3 本地 DNS/VPN 干扰

全局 VPN、TUN、加速器或劫持 DNS 的安全软件可能让本机解析结果与公网不同。菜单 `[1]` 等待 DNS 时，先暂停会接管 DNS/SSH 的全局隧道，再用多个公共解析器核对。不要因本地缓存错误而反复删除正确 DNS 记录。

## 10. 从旧版本升级

### 10.1 从 v0.9.0

直接使用重置版 v0.9.5 的 `[1]`。程序读取旧本地历史、Host Key 与 key 位置，远端先备份，再按明确计划升级。不要先手工删除 v0.9.0 数据，否则会失去兼容迁移依据。

### 10.2 从旧实验版 v0.9.5

旧版与重置版都显示 v0.9.5，但重置版内部 revision 更高。`[1]` 会：

1. 识别旧内部构建；
2. 备份当前节点和旧实验组件状态；
3. 停止并移除设备准入/controller/邀请/网盘/local-admin 的受管服务和程序；
4. 保留 x-ui/Xray、Nginx、证书、Reality/CDN、SSH、凭据和备份；
5. 不把旧网盘用户文件根直接删除；
6. 按新 InstallPlan 收敛。

被保留的旧网盘数据不会出现在新客户端，也不会继续提供网盘服务。先离线核对和另行备份，再由本人决定是否删除。

## 11. Windows 与 Android 差异

| 项目 | Windows | Android |
|---|---|---|
| SSH 实现 | 系统 OpenSSH | 应用内 SSH/SCP |
| 密钥保护 | 用户 SSH 目录、系统 ACL | Android Keystore + 应用私有存储 |
| 服务商秘密 | 可选 Windows Credential Manager | Keystore 加密仓 |
| 本地 10808 | 可管理当前用户环境变量 | 仅让本应用的服务商 API 请求走 10808，不接管 SSH/系统流量 |
| 文件导出 | 常规下载目录/选择路径 | 系统文件选择器或分享面板 |
| 面板隧道 | 本地进程持有 | 前台服务持有，系统通知可停止 |
| OpenSSH 安装 | 可一次性安装验证 | 不需要系统 OpenSSH |

Android 包体通常小于 Windows 单 EXE，因为 Windows 自包含 .NET/WPF 运行依赖，而 Android 依赖系统运行环境；包体大小不能用来判断功能是否齐全。正式包必须内嵌与 Windows 同哈希的远端工具包。

## 12. 失败处理

| 现象 | 正确动作 |
|---|---|
| DNS 检测不到刚创建的记录 | 关闭 TUN/全局代理，清缓存，换公共解析器核对，不要盲删记录 |
| SSH Host Key 变化 | 从服务商 Console 核对；确认重装后再替换 |
| 远端命令非零 | 不继续；复制日志，运行 `[3]` |
| 交接单为空 | 不复制；检查远端步骤和结构化标记 |
| 面板白屏 | 确认隧道仍活着、端口/路径来自最新元数据；不要公网暴露面板 |
| Reality `24443` 不通 | 拒绝提升 443；查防火墙、Xray 日志、客户端参数和本地链路 |
| Orange 边缘通、客户端不通 | 核对订阅必须是橙云域名 `:8443`、TLS、XHTTP、正确 path/host/SNI |
| 源站 `8443` 公网可直拨 | 视为失败；检查 Cloudflare CIDR 防火墙锁，不得提交拓扑 |
| 同版本反复安装 | 不要重跑；相同完整构建应跳过，文件损坏按提示 `[13]` 后 `[1]` |
| 点击停止后无响应 | 等待当前 SSH 清理；若底层进程不退出再强制结束，之后运行 `[3]` 核对事务状态 |

## 13. 上线验收清单

- [ ] 使用的是重置版 v0.9.5，界面没有设备准入或网盘；
- [ ] VPS Host Key 已核对；
- [ ] 已保存可用的 SSH 登录方法和服务商 Console 救援方法；
- [ ] 线路模式不是空回车产生的默认值；
- [ ] 域名与证书邮箱由本人输入并在预览中核对；
- [ ] 模板、性能、WARP 选择明确；
- [ ] 端口组显示 `443 / 24443 / 8443 / 40000`；
- [ ] 输入 `APPLY` 前没有上传；
- [ ] 已有节点变更前备份成功；
- [ ] Reality/Orange 对应真机验货真实通过；
- [ ] 失败分支没有复制交接单或打开面板；
- [ ] 交接单已进入密码管理器，剪贴板已清空；
- [ ] 菜单 `[3]` 诊断通过；
- [ ] 灾备与服务商 Console 入口均可找到。

## 14. 安全与合法使用

只在本人拥有或得到明确授权的 VPS 上使用。不要把诊断报告、交接单、私钥、API Key、备份或运行态输入文件上传到公开仓库、公开 issue 或群聊。使用者应遵守所在地区法律以及 VPS、域名和 CDN 服务商条款。
