# TextNodeAssistant v0.9.5 完整使用说明书

适用：Windows x64/x86/ARM64、Android universal APK、远端 `text-node-assistant v0.9.5` 工具包。

## 1. 产品边界

TextNodeAssistant 是双层私人网盘与 VPS 图形运维客户端：

- 外层：普通网盘账号注册/登录、admin 登录、文件操作、改密、节点切换；
- 内层：SSH、安装、拓扑、面板、诊断、安全、设备、备份、拆除与恢复；
- 远端：固定供应链工具包、3x-ui、Xray、Nginx、copyparty、WARP、vnStat、Fail2ban；
- 所有施工只针对用户拥有或明确授权的 VPS。

## 2. 安全模型

### 2.1 本机 admin

本机 admin 可直接登录外层 admin 空间。进入高级控制台默认再次验证 admin，可在内层关闭门禁。首次创建或改密时生成恢复包和恢复码；修改 admin 必须进内层。

Windows 使用 Credential Manager/DPAPI 和加盐验证器；Android 使用 Keystore 加密应用仓。恢复包与恢复码应分开保存。

### 2.2 普通网盘账号

- 注册入口在外层登录页；
- 远端未完整施工时禁止注册，避免幽灵账号；
- 每台 VPS 最多 2 个普通账号；
- 普通账号属于该 VPS/空间，可在所有受信设备登录；
- 外层可输入旧密码并二次确认新密码改密；
- 切换网盘 VPS 会退出登录，必须输入目标节点账密。

### 2.3 SSH

每项远端操作重新选择临时密码或节点/设备 key。密码只通过受保护输入通道交给 OpenSSH；key 按 NODE_ID、用户和设备隔离。新 key 必须真实登录成功后才撤旧 key。

Host Key 指纹变化必停。厂商 VNC/串口/Console 是严格安全模式下的恢复兜底。

### 2.4 设备准入

设备准入限制服务器管理权；设备订阅是另一套凭据。邀请在首次成功绑定后才消费，网络中断可重试。最后一个 controller 不允许直接吊销。

## 3. 本地首页

未选择/未施工 VPS 时显示“未挂载节点”，提供：

- 创建/恢复本机 admin；
- 外层普通账号登录或注册（注册需远端完整）；
- admin 登录；
- 无需预登录的设备邀请响应入口；
- 进入高级控制台施工；
- 节点历史选择和一键删除历史。

节点历史只保存地址、SSH 用户和端口，不保存密码或私钥。

## 4. 高级控制台菜单

| 操作 | 名称 | 说明 |
|---:|---|---|
| 1 | 安装/升级/恢复/自适应优化 | 唯一施工和拓扑变更入口 |
| 2 | 打开 3x-ui | 127.0.0.1 SSH 隧道，不暴露公网面板 |
| 3 | 自动体检 | SSH、服务、端口、订阅、证书、网盘结构化诊断 |
| 4 | 安全自动修复 | 先备份，只修复可判定项 |
| 5 | VPS 密码轮换 | 显示真实新密码并复制 |
| 6 | 3x-ui 凭据轮换 | 真登录验证后写交接单 |
| 7 | 当前交接单 | 缺字段或验证失败禁止复制 |
| 8 | 伪装网站 | 15 套模板，随机/稳定/指定 |
| 9 | 完整灾备 | 含程序、身份和配置，体积较大 |
| 10 | 紧急报告 | 下载前脱敏，分享前再次检查 |
| 11 | SSH key | 绑定、轮换、验证、撤旧 |
| 12 | 清空剪贴板 | 纯本地 |
| 13 | 卸载远端工具包 | 保留业务、配置、凭据与备份 |
| 14 | 本地 10808 环境变量 | 配置、撤销、查看，不登录 VPS |
| 15 | 当前配置单备份 | 清理多余受管备份，仅保留一份 |
| 16 | 自适应性能 | 按 CPU/内存/磁盘评估可回滚参数 |
| 17 | 流量中心 | vnStat 估算或服务商只读 API |
| 18 | 拆除施工和恢复基线 | 仅拆代理或整体恢复 |
| 19 | 安全事件 | 有界、脱敏安全日志 |
| 20 | 设备准入 | 邀请、绑定、暂停、恢复、吊销、节点 |
| 21 | 强制网盘 | 隧道、账号、改密、恢复能力 |
| 22 | 拓扑只读状态 | 显示灰/橙/双路，不施工 |
| 23 | 公网 IP 重绑定 | 多身份一致才提交 |
| A/B | 本机 admin | 改密/恢复/门禁与超时 |

## 5. 操作 `[1]` 详细流程

### 5.1 版本识别

- 未安装：上传并 bootstrap；
- 同版本同构建：跳过上传与重复安装，进入状态收敛；
- 远端旧版：备份、卸旧工具包、上传新包、迁移状态；
- 远端新版：拒绝降级，提示使用更新客户端；
- 业务存在但工具包缺失：只恢复工具包和状态索引，不盲目重装业务。

### 5.2 原生基线与事务

第一次可信施工前保存服务、端口、防火墙、sshd、包、Nginx 和受管文件基线。每次施工有事务 ID、阶段、预期状态、回滚动作和最终回执；失败时回滚已提交阶段。

### 5.3 必选拓扑

新机和已拆节点必须选 1–3，空输入无效。只有识别到完整既有部署才显示 0 保持。

| 模式 | 灰云域名 | 橙云域名 | 链路 |
|---|---:|---:|---|
| 仅灰云 | 1 | 0 | VLESS + REALITY 直连 |
| 仅橙云 | 0 | 1 | VLESS + XHTTP + Cloudflare |
| 双路 | 1 | 1 | 两条独立节点/订阅 |

三模式互切由同一入口先准备新链路、真实验证、原子提交，再拆旧链路。

### 5.4 强制网盘

网盘始终安装并验收，随机选择 `39000—39999` 回环端口，不需要域名，不开放公网端口。固定 copyparty 供应链并以非 root 用户运行。

### 5.5 完整交接单

交接单必须如实包含：VPS 地址、SSH 用户/端口/认证状态，3x-ui 用户名、真实可验证密码、端口、WebBasePath，线路和当前设备节点，网盘隧道/账号，以及必须保存的恢复材料。

旧面板只有哈希而无明文时显示 `PASSWORD_UNKNOWN_NOT_RECOVERABLE`，由用户确认轮换，绝不伪造密码。

## 6. 灰云模式

DNS A 记录为 DNS only 并指向 VPS。程序用多个公共解析器交叉验证。Windows 开启 v2rayN TUN、VPN、私人 DNS 或系统代理时可能劫持 DNS，应暂停后重测。

REALITY 先在 24443 创建影子并要求真机浏览，确认后才晋升生产端口；重复执行会识别已有等价入站，不覆盖未知入站。

## 7. 橙云模式

Cloudflare 必须：

1. hostname 为 Proxied；
2. Universal SSL Active；
3. SSL/TLS `Full (strict)`；
4. 客户端使用该 hostname 的 `8443` 端口；Cloudflare 免费计划不需要 Origin Rule；
5. Cache Rule：Bypass cache；
6. 无 Access、质询、重定向或 Worker 拦截。

程序准备源站证书，只允许 Cloudflare 官方 CIDR 访问 8443，验证 `Cf-Ray`、受管源站标记和直连源站失败。真机导入 8443 XHTTP 链接并浏览成功后才提交。

Universal SSL 只覆盖客户端到 Cloudflare Edge；Full (strict) 仍验证 Edge 到 VPS 的源站证书。

## 8. 双路模式

双路使用两个不同 hostname。灰云和橙云拥有独立链接；一条故障时另一条不应被自动覆盖。切换到单路仍通过 `[1]`，程序确认目标链路可用后再拆除另一条。

## 9. 3x-ui 面板

`[2]` 读取真实 panel 元数据，建立 `127.0.0.1:随机端口` SSH 隧道并打开正确 WebBasePath。关闭使用专用“关闭面板隧道”，应用退出或清空当前选择也会收尾。

白屏排查：保持隧道进程，确认本地端口、WebBasePath、x-ui active 和 localhost 登录探针；不能从公开 VPS 地址打开面板。

## 10. 网盘

`[21]` 提供：状态、打开/关闭隧道、注册普通账号、登录、改密、恢复 admin 能力、查看回环端口。外层文件传输使用当前登录节点；切换节点自动退出。

每个普通账号默认使用保守配额，按磁盘容量和保留空间自适应提高。注册前同时验证 deployment complete、drive healthy 和 current device active。

## 11. 设备准入菜单

推荐顺序：

1. 刷新状态；
2. controller 创建邀请；
3. 新设备未登录首页响应邀请；
4. controller 批准；
5. 新设备首次真实绑定；
6. controller 查看设备 active；
7. 新设备显示并复制自己的节点。

邀请批准不等于消费；首次绑定成功才失效。可暂停、恢复、吊销设备，但最后 controller 受保护。

## 12. 密码和 key 管理

- VPS/面板随机密码完整显示并可复制，用户应立刻保存；
- 修改 VPS 密码前必须已有另一条可验证 SSH/Console 恢复路径；
- key 轮换先安装新公钥、真实登录，再移除旧公钥；
- “全部转入备份态”会清空活动绑定槽但保留本机可恢复备份；
- 恢复 key 时选择备份目录，验证公私钥匹配，再用密码登录一次实测；
- 服务器 SSH host 私钥绝不导出，只显示公钥指纹。

## 13. 备份与报告

- 当前配置备份：仅必要配置、凭据元数据和状态；
- 完整灾备：含程序、身份和恢复资料；
- 救援包：拆除前强制生成并下载校验；
- 紧急报告：结构化诊断，默认脱敏；
- 日志仍可能含域名、用户名、端口和节点元数据，分享前人工复查。

## 14. 流量中心

SSH/vnStat 能看到 VPS 网卡累计和周期估算，不等于厂商套餐账单。KiwiVM/SolusVM/RackNerd 等只读 API 可配置：Key/Token 只临时输入，或经确认保存在 Windows Credential Manager/Android 安全仓，不写普通日志、参数或 JSON 配置。

## 15. 本地 10808

`[14]` 只操作本地环境：

```powershell
$env:HTTP_PROXY="http://127.0.0.1:10808"
$env:HTTPS_PROXY="http://127.0.0.1:10808"
$env:NO_PROXY="localhost,127.0.0.1,::1"
```

可配置、撤销或查看，不登录 VPS。它不等于 v2rayN TUN；DNS 探测异常时仍需暂停 TUN/VPN。

## 16. 拆除施工和恢复基线

`[18]` 先做救援包、SHA-256 下载校验和计划预览，再要求精确危险确认。

- 代理存在：可仅拆代理并保留网盘，或整体拆除；
- 代理不存在、只剩网盘：才显示拆除剩余网盘；
- 整体拆除：撤销代理、网盘、受管服务与文件，并恢复可信原生基线；
- 旧节点无可信快照：标记 `LEGACY_UNCERTAIN`；
- 恢复施工：只运行 `[1]`。

## 17. 公网 IP 重绑定

IP 变化不自动接受。只有 Host Key、machine-id、NODE_ID、SERVER_ID 和受管状态匹配，且目标不冲突时才提交。失败不改本地历史和 key 目录。

## 18. OpenSSH 启动门禁

Windows 启动时：

1. 查找 ssh、scp、ssh-keygen、ssh-keyscan；
2. 已安装则执行能力和版本验证；
3. 未安装则申请管理员权限安装 Windows OpenSSH；
4. 安装后只做一次明确验证；
5. 失败给出系统组件日志和手动修复，不进入无限重试。

Host Key 扫描会按兼容算法分层探测；单个 KEX 不支持不会导致整台主机误报，但零有效 key 或指纹变化仍停止。

## 19. 失败即停协议

一次远端操作只有同时满足以下条件才算成功：

- SSH/远端命令退出码为 0；
- 有本次事务对应的结构化成功标记；
- handoff 非空且包含必填字段；
- panel/drive/订阅有真实功能探针；
- 远端状态回读与预期一致。

`Connection closed`、SSH banner、stderr 摘要或空 stdout 都不能被当成交接单。

## 20. 数据位置与迁移

新版本使用 TextNodeAssistant 命名的当前用户数据目录和安全存储目标。旧 ProxyNodeAssistant 路径只用于一次性只读迁移：验证内容、导入新格式、写迁移回执，不回写旧目录。源码包和便携包不含任何运行态数据。

Android 卸载会移除 Keystore 应用密钥；Windows 删除凭据前应先导出恢复包。

## 21. 发布隐私

禁止进入源码/成品：真实 VPS、域名、邮箱、密码、Token、私钥、完整订阅、邀请响应、交接单、服务商 API Key、Android 签名密钥。

发布门禁扫描源码和解包后的 EXE/APK/ZIP/TAR，并输出 SHA-256 和 SPDX SBOM。

## 22. 常见故障树

| 症状 | 优先处理 |
|---|---|
| DNS 无穷重检 | 暂停 TUN/VPN/私人 DNS；公共解析器重测 |
| SSH 密码拒绝 | 用户/大小写/空格/RootLogin；用 Console 重置 |
| ssh-keyscan KEX 错误 | 使用新版客户端兼容探测；仍无 key 则查端口/sshd |
| 施工返回非零 | 复制日志，运行 `[3]`，再由 `[1]` 恢复事务 |
| 面板元数据为空 | `[3]` 检查 x-ui 与元数据脚本；禁止猜端口 |
| 面板白屏 | 保持隧道、核对本地 URL/WebBasePath、关闭代理绕过 localhost |
| 网盘未挂载 | 目标节点未完整施工；进入 `[1]` 恢复 |
| 普通账号无法注册 | 部署未 complete、设备未 active 或已满 2 个账号 |
| 橙云 404/403 | 确认客户端使用 `:8443`、Cache Bypass、无 Access/Worker/质询/重定向，且源站证书与路径正常；免费计划不需要 Origin Rule |
| 订阅延迟 -1 | 获取当前 active 设备节点，检查服务/防火墙/客户端核心 |
| 停止按钮不退出 | 等待有限清理；超时强制结束后运行 `[3]` |
| 交接单无真实密码 | 旧哈希不可逆，主动轮换并真实登录验证 |

## 23. 发布前验收

必须通过：Windows x64/x86 运行时冒烟、ARM64 交叉构建与静态门禁、Android 单元测试和签名构建、Linux Shell 静态测试、工具包哈希一致、源码/产物隐私扫描、源码包排除签名材料，以及真实 VPS 的安装/灰云/橙云/双路/切换/设备/网盘/拆除/恢复矩阵。

从零购买和 Cloudflare 图示流程见[从零部署教程](TextNodeAssistant-v0.9.5-从零部署教程.md)。
