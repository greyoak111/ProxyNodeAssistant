# TextNodeAssistant v0.9.5 Android 使用说明

Android 版是 Kotlin + Jetpack Compose 原生客户端，不依赖 Termux、WebView 或外部 SSH 应用。它与 Windows 版使用相同的远端工具包、NODE_ID、设备准入协议和失败即停规则。

> APK 为 universal 架构，最低 Android 7.0。应用 ID 和旧签名身份为兼容已安装用户而保留；界面、文件名和产品名称统一显示 TextNodeAssistant。

## 1. 安装与升级

1. 下载 `TextNodeAssistant-v0.9.5-android-universal.apk`。
2. 对照发布目录的 `SHA256SUMS-v0.9.5.txt` 核验 SHA-256。
3. 允许当前文件管理器安装未知来源应用。
4. 直接安装；已有正式版可原地升级，不要先卸载，否则 Android Keystore 中的本机设备身份会随应用数据丢失。
5. 首次启动创建本机 admin，并立即导出恢复包与恢复码。

## 2. 双层界面

启动先进入网盘外层：

- 普通账号：登录当前 VPS 的个人空间；
- admin：也能直接登录外层 admin 空间；
- 高级控制台：默认需要再次验证本机 admin，验证后才显示完整运维功能；
- 未挂载节点：如实显示未挂载，并引导去高级控制台施工或使用首页设备邀请入口。

高级控制台二次门禁默认开启，可在内层关闭，但不建议在共享手机上关闭。普通网盘账号可用旧密码并二次确认新密码在外层改密；admin 必须进入内层改密。

## 3. SSH 登录模式

每个远端操作都会重新选择目标和认证方式：

1. **临时密码**：密码只存在当前内存会话，成功后使用一次性 SSH key，结束即撤销；
2. **节点长期 key**：按 NODE_ID、SSH 用户和设备隔离，先验证新 key 再撤旧 key；
3. **受信设备 key**：通过邀请完成首次绑定后使用，不需要把服务器密码交给新设备。

首次连接必须核对 VPS SSH Host Key 指纹。指纹变化时应用会失败即停；请先在厂商控制台确认是否发生重装、迁移或被劫持，不能盲目接受。

## 4. 新设备如何加入

新设备不需要先登录同一台 VPS：

1. 已有 controller 打开高级控制台 `[20]`，创建 10 分钟邀请；
2. 新手机在未登录状态首页选择“响应设备邀请”，粘贴邀请；
3. 新手机生成本机独享设备身份和 SSH key，输出响应；
4. controller 在 `[20]` 批准响应；
5. 新手机再次打开邀请入口完成首次真实 key 登录；
6. 只有首次绑定成功后邀请才消费，网络中断或审批未完成时可继续重试。

严格安全模式启用后，未获准设备直接 SSH 会被拒绝；厂商 VNC、串口或救援 Console 仍是兜底。

## 5. 唯一施工入口 `[1]`

只有 `[1]` 能上传/升级工具包、恢复缺失组件、安装强制网盘或改变线路拓扑。它会：

1. 验证 Host Key 与 SSH；
2. 识别远端构建：同构建跳过，旧构建升级，远端更新则拒绝降级并建议换新客户端；
3. 迁移旧状态并保存原生基线；
4. 为当前设备更新独享 key；
5. 明确要求选择线路，空输入无效；
6. 安装并验收强制网盘；
7. 施工灰云、橙云或双路；
8. 创建/恢复 controller、生成完整交接单并原子提交。

| 选项 | 线路 |
|---|---|
| 1 | 仅灰云：DNS-only 子域名，REALITY 直连，路径短但源站 IP 可见 |
| 2 | 仅橙云：Proxied 子域名，XHTTP 经 Cloudflare 免费支持的 :8443，需 SSL/Cache 配置 |
| 3 | 双路：两个不同子域名，两条订阅独立存在，切换和容灾最清楚 |
| 0 | 保持：只在检测到已施工且状态完整的节点显示 |

所有模式互切仍回到 `[1]`。`[22]` 只显示当前拓扑和验证结果，不再独立改线。

## 6. Cloudflare 引导

橙云或双路模式会逐项暂停并要求确认：

1. 该子域名已开启 Proxied；
2. Universal SSL 已签发并覆盖该 hostname；
3. SSL/TLS 模式为 `Full (strict)`；
4. 客户端使用该 hostname 的 `8443` 端口；免费计划无需 Origin Rule；
5. Cache Rule 对该 hostname使用 Bypass；
6. 未附加会拦截 XHTTP 的 Access、质询、重定向或 Worker。

每项完成后回应用继续；输入 `q` 安全取消。程序会准备源站证书、限制 8443 只接受 Cloudflare 官方 CIDR，并用 `Cf-Ray`、受管源站标记和直连拒绝做双端验收。Universal SSL 只负责客户端到 Edge，`Full (strict)` 仍要求源站证书有效。

## 7. 强制网盘

网盘是施工基线的一部分，不再是可选组件：

- copyparty 固定版本、下载地址、文件大小和 SHA-256；
- 非 root systemd 用户运行；
- 随机选择 `39000—39999` 的回环端口，不占公网端口；
- 只通过 SSH 隧道访问，不需要网盘子域名；
- 每台 VPS 最多注册 2 个普通账号，只有完整施工且当前设备受信时才开放注册；
- 普通账号在同一 VPS 的所有受信设备上访问同一空间；
- 切换网盘 VPS 会自动退出并要求目标 VPS 的网盘账密。

本机 admin 能力保存在 Android Keystore 加密仓。普通网盘账号的可恢复秘密使用每个 controller 的加密信封保存；VPS 不保存可导出的明文恢复凭据。

## 8. 高级控制台功能表

| 编号 | 功能 |
|---:|---|
| 1 | 安装、升级、恢复、三种拓扑收敛 |
| 2 | 通过 SSH 隧道打开 3x-ui |
| 3–4 | 自动体检、安全修复（先备份） |
| 5–7 | VPS/面板凭据轮换与完整交接单 |
| 8 | 15 套伪装模板随机/稳定/指定 |
| 9–10 | 完整灾备、紧急诊断报告 |
| 11 | 绑定或轮换 SSH key |
| 13 | 仅卸载远端工具包，不重装业务 |
| 15–17 | 当前配置备份、性能、vnStat 流量估算 |
| 18 | 拆除施工和恢复基线 |
| 19 | 安全事件读取 |
| 20 | 设备准入、暂停、恢复、吊销、设备节点 |
| 21 | 强制网盘、账号、改密、凭据恢复 |
| 22 | 线路拓扑只读状态 |
| 23 | 公网 IP 重绑定 |

## 9. 拆除与恢复

`[18]` 先生成并校验救援包，再根据现场显示：

- 仅拆代理：保留强制网盘；
- 整体拆除：撤销代理、网盘和受管施工，恢复原生基线；
- 拆除剩余网盘：只在代理已不存在时出现；
- 恢复施工：回到 `[1]`，根据回执和现场只补缺失部分或完整收敛。

没有可信基线的旧节点会标记 `LEGACY_UNCERTAIN`，不会伪称完全恢复原状。

## 10. 凭据交接单

完整交接单至少包含 VPS 地址、SSH 用户和端口、当前认证状态、面板用户名/真实可验证密码/端口/WebBasePath、当前设备节点、订阅、网盘隧道信息和必须保存的 admin 恢复提示。

无法从哈希还原的旧面板密码会明确标记未知，并要求主动轮换；绝不输出假密码。交接单为空、缺字段或真实登录探针失败时不允许复制。

## 11. 常见故障

- 密码总被拒绝：核对 SSH 用户、大小写和粘贴空格；厂商可能禁用 root 密码。用厂商 Console 重置后再试。
- DNS 一直不通过：手机关闭 VPN/TUN/私人 DNS 后，用公共解析器重试。
- 橙云失败：核对 Proxied、Universal SSL、Full (strict)、客户端是否使用 :8443、Cache Bypass。
- 面板或网盘打不开：保持应用和 SSH 隧道存活，只用应用给出的 `127.0.0.1:随机端口`。
- 连接中途关闭：复制脱敏日志，运行 `[3]`；不能把 `connection closed` 当成交接单。
- 订阅延迟 -1：在 `[20]` 重新复制当前设备节点，检查设备是否 active、服务器端口和客户端核心。

## 12. 本地数据与恢复

卸载应用或清除应用数据会删除 Android Keystore 密钥。因此每次创建/更换 admin 或设备 key 后立即导出恢复包；恢复包和恢复码分开保存；不要截图或上传完整交接单、私钥、邀请响应和恢复码；换机优先使用设备邀请建立新身份。

## 13. 构建

```powershell
cd android
.\build-android.ps1 -Task Test
.\build-signed-release.ps1
```

输出为 `android\dist\TextNodeAssistant-v0.9.5-android-universal.apk`。签名密钥、密码文件、`local.properties`、Gradle 缓存和构建目录不得进入源码包。为保持已安装用户可升级，正式发布使用既有受保护签名身份。
