# v0.8.2

- 修复 WPF 图形直达操作打开 3x-ui 后后台立即退出、SSH 隧道过早关闭，浏览器只取得 HTML 标题而因 JS/CSS 请求失败显示白屏的问题。
- 隧道生命周期按“是否实际创建了隧道”统一管理，不只覆盖菜单 `[2]`；菜单 `[1]` 施工后选择打开面板及未来复用入口同样进入保持状态。
- GUI 在保持期间显示绿色状态和“关闭面板隧道”按钮；点击、退出或安全停止才执行清理。
- 操作失败、未创建隧道或浏览器打开失败时不会进入假保持状态。
- 继续保持全图形工作区、受限 AskPass 命名管道、明确 SSH 双模式选择和隐私扫描。
