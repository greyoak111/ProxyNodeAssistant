# v0.6.5

- 菜单 `[1]` 新增显式远端版本状态机。
- 同版本禁止重复上传/bootstrap；构建标识不同只提醒，不自动覆盖。
- 旧版本由 `[1]` 升级，验证新版本后再清理旧程序。
- 远端版本高于 EXE 时禁止降级并停止后续施工，提示更换新版 EXE。
- 同版本但安装不完整时，要求用户明确执行 `[13] → [1]`，避免循环重装。
- 无法识别版本时失败关闭，不上传任何内容。
- 版本按数字段比较，正确处理 `0.6.10` 与 `0.8.1`。
- 卸载白名单扩展至 v0.6.5，并保留 v0.6.4 的安全边界。

