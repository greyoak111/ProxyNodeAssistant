# v0.6.4

- EXE 菜单 `[1]` 明确标为唯一安装入口；仅该分支可以上传并 bootstrap 远端内嵌包。
- 新增菜单 `[13]` 安全卸载远端管理工具，要求输入 `UNINSTALL`。
- 卸载在任何删除前校验全部目标归属和文件类型；异常时整体拒绝。
- 卸载保留节点服务、配置、凭据、证书和灾备。
- `[1]` 安装成功后清理已知旧版工具包目录及临时上传包，避免重复版本堆积。
- 保留 v0.6.3 的 SSH 流分离、失败硬停止、交接单/panel 元数据校验、结构化诊断及 rc=141 修复。
