# ProxyNodeAssistant v1.0.0 更新说明

## 版本性质

本仓库的 v1.0.0 是“精简重置版”：以稳定的 v0.9.0 为代码基线重新迭代，不是旧实验版 v1.0.0 的继续堆功能。

可见版本号仍为 `v1.0.0`，但重置线使用更高的内部构建修订。菜单 `[1]` 会同时比较版本、内部修订、构建标识和文件完整性，因此能区分旧实验版、当前重置版与更新的远端构建。

## 已彻底取消

- 设备准入、controller/member、邀请、租约、围栏和“受信设备才能管理”的门限；
- 私人网盘、Copyparty、普通网盘账户及其远端恢复/凭证托管；
- 本机 admin、加密恢复包、UI 门禁和设备身份状态；
- 依赖上述功能的服务、菜单、页面、状态机、灰色占位按钮和成功判定。

节点长期 SSH key 仍保留，但它只是当前电脑或手机登录某台 VPS 的普通凭据，不是设备身份、准入资格或 controller 权限。临时 SSH 密码同样可用。

旧文档中出现的“创建邀请”“响应邀请”“批准设备”“进入私人文件空间”等流程全部作废。

## 从 v0.9.0 保留的稳定能力

- 每一项远端操作重新选择目标与 SSH 登录方式：临时密码或按 VPS + SSH 用户隔离的长期 key；
- Host Key 固定、OpenSSH 组件校验、KEX 兼容回退与失败关闭；
- 3x-ui/Xray、Nginx、Reality、Cloudflare/XHTTP、WARP、证书和本地回环面板隧道；
- 诊断、安全修复、凭据交接单、完整灾备、紧急报告、备份整理、性能档、流量估算和服务商流量 API；
- 15 套本地伪装模板、历史 VPS、密钥备份/恢复、中英双语；
- 完整凭据交接：VPS/面板账号、Reality、SS2022 TCP 与灰云/橙云 XHTTP 链接按实际启用线路输出；
- Windows 原生 GUI/CLI 和 Android 原生客户端；Android 可自行把内嵌工具包上传到全新 VPS。

发行包同时提供 macOS/Linux 无 GUI CLI；使用系统 OpenSSH，与其他客户端复用同一远端工具包和施工语义。

## 菜单 `[5]` / `[6]`：随机或自定义凭据

这两个独立维护菜单不再只是“随机化”按钮：

- `[5]` VPS 登录密码：选择 `1` 生成随机密码、`2` 输入并二次确认自定义密码，或 `0` 取消；
- `[6]` 3x-ui 账号/密码：选择 `1` 生成随机账号密码、`2` 输入并二次确认自定义账号密码，或 `0` 取消；
- 自定义密码只通过本次 SSH 会话写入随机命名的 root-only `0600` 一次性文件，脚本和客户端在成功、失败、取消或中断时清理；不写入设置、命令行或普通日志；
- 只有远端实际更新并且完整交接单通过校验后，才显示或复制真实凭据；取消不会修改 VPS。

## 菜单 `[1]`：唯一施工入口

只有 `[1] 安装 / 升级 / 自适应优化` 可以上传、安装、替换或升级远端工具包。其他菜单只执行各自明示的维护动作，不会暗中重装。

### 必须明确选择线路

| 输入 | 线路 | 适用范围 | 结果 |
|---:|---|---|---|
| `0` | 保持现有线路 / Keep | 仅已有节点显示 | 保留当前线路，不拆、不换 |
| `1` | 仅灰云 / Gray | 新机或已有节点 | DNS only + Reality |
| `2` | 仅橙云 / Orange | 新机或已有节点 | Cloudflare + TLS + XHTTP |
| `3` | 双路 / Dual | 新机或已有节点 | 灰云 Reality 与橙云 XHTTP 并存 |

空回车无效。灰云和橙云所需的域名、证书邮箱分别手填，没有包内默认值。双路必须使用两个不同主机名。

### 其他明确选择

- 伪装模板：已有节点可保留；或选 `R` 随机、`A` 按域名稳定、`1—15` 指定；
- 性能：已有节点可保留；或自动、低配、标准、高配；
- WARP：保持现状或确保开启；不会因空输入静默安装；
- 备份：变更前强制执行，不能关闭；
- 成功收尾：可选清理多余备份、可选打开面板，失败时两项均不执行。

### 固定联调端口组

- Reality 正式：`443`；
- Reality 真机验货：`24443`；
- Cloudflare/XHTTP 边缘与受限源站：`8443`；
- WARP 本地回环：`40000`。

端口作为一个整体固定，避免只改一处后造成 Xray、Nginx、防火墙、订阅导出、诊断或回滚互相不一致。

## 先预览，再上传

菜单 `[1]` 的新顺序：

1. 选择 VPS 与 SSH 方式；
2. 只读识别远端工具包、内部构建和现有节点；
3. 收集完整线路、身份、模板、性能、WARP 与收尾计划；
4. 本地校验并显示完整预览，邮箱局部遮罩；
5. 只有精确输入大写 `APPLY` 才开始上传和远端修改。

任何其他输入都会取消，取消前不会上传工具包。

施工参数通过随机文件名、仅 root 可读、权限 `0600` 的一次性输入文件传给远端，用后删除。域名和邮箱不会写入通用偏好或发行包。

## 真机验货与事务回滚

- 已有节点先备份，再改动；
- Reality `24443` 和 Cloudflare/XHTTP `8443` 仍需把打印的链接导入真实客户端并实际浏览；
- 工具不会把端口可达自动冒充为“真机可用”；
- 橙云或双路先暂存新拓扑，通过结构、边缘回源和真机验货后才提交；
- 任一步失败立即硬停止，恢复原运行态，不复制空交接单、不清理备份、不打开面板。

## 升级与迁移

### 从 v0.9.0 升级

运行新版菜单 `[1]`。程序先备份现有节点，再升级远端工具包，并按本次明确选择的线路计划施工。现有 3x-ui 节点不会因工具包升级被无条件重装。

### 从旧实验版 v1.0.0 升级

菜单 `[1]` 通过内部修订识别旧构建，执行一次有界退役：

- 归档并停止旧设备身份/准入、网盘和本机 admin/UI 门禁元数据；
- 删除由旧实验功能创建的准入与网盘服务、规则和程序；
- 不把旧实验状态迁移到活动配置；
- 保留现有 x-ui 节点、凭据、SSH 登录和与本次线路计划无关的数据；
- 安装重置线工具包，然后按新的显式计划继续。

重置版不会重新启用旧网盘、本机 admin 或 UI 门禁，也没有对应文件浏览或恢复入口。

### 同版本与新版本保护

- 远端是相同完整重置构建：跳过工具包上传，只执行本次明确选择的计划；
- 远端同版本但内部修订较旧：进入 `TOOLKIT_ONLY_UPDATE_REQUIRED`，只更新工具包构建；确认大写 `APPLY` 后依次记录 `TOOLKIT_ONLY_UPDATE_CONFIRMED` 与 `TOOLKIT_ONLY_UPDATE_COMPLETE`，不收集线路/凭据/面板设置，也不运行全量安装器；
- 远端构建更新：拒绝降级，提示换用匹配或更新的客户端；
- 同版本但文件不完整：菜单 `[1]` 在 `APPLY` 确认后原位修复管理工具；其他菜单不会自动上传；若修订更高或构建 ID 不同仍拒绝覆盖。

## 卸载与恢复基线的区别

### `[13] 卸载远端内嵌包`

仅移除 ProxyNodeAssistant 的远端程序包和启动入口。保留 x-ui/Xray、Nginx、WARP、线路、证书、凭据与备份。之后只有菜单 `[1]` 可以重新安装管理工具包。

### `[18] 全量拆除并恢复原始基线`

这是高风险恢复操作：先下载并校验救援包，再显示拆除计划并要求精确确认；有安装前基线时按基线恢复，没有逐字节基线的旧节点还需要第二次 legacy 全拆确认。SSH 救援通道必须保留。

两项都不会把 SSH 配置、节点凭据或普通用户文件当作工具包垃圾直接删除。

## Windows 与 Android 差异

| 项目 | Windows | Android |
|---|---|---|
| SSH/SCP | 经验证的 Windows OpenSSH | 应用内 SSH/SCP |
| 长期 key | 本机 SSH 目录及可恢复归档 | Android Keystore 保护的密钥仓 |
| 服务商 API 密钥 | 经同意后进 Credential Manager | 经同意后进 Keystore 加密仓 |
| 界面 | WPF GUI，保留 CLI/日志视图 | 原生 Android 页面 |
| 剪贴板 | Windows 剪贴板 | Android 剪贴板/分享 |
| 本地 10808 `[14]` | 当前用户环境变量 | 应用内开关，仅作用于服务商 API 请求；不接管 SSH/系统流量 |

两端远端施工边界相同：没有本机 admin/恢复、UI 门禁、设备身份/准入、controller、邀请、租约或网盘；菜单 `[1]` 使用同一组线路、模板、性能、端口和 `APPLY` 语义。当前 Android 对全新节点只接受 `WARP=确保开启`，已有节点才允许 `保持现状`；Windows 会显示两项并要求明确选择。

## 兼容性破坏提示

- 不能再空回车采用线路、模板、性能或 WARP 默认值；
- 不能在发行包或偏好中预填域名、邮箱或 VPS；
- 不能使用旧设备邀请或网盘页面；
- 不能在菜单 `[1]` 自由拆分修改四个协调端口；
- 不能在真实客户端未验证时提交危险线路切换。

## 发布前自检标准

- 源码、Windows 包、Android 包和远端归档均无真实 IP、域名、邮箱或秘密；
- 活跃界面与工作流无本机 admin/恢复、UI 门禁、设备身份/准入、controller、邀请、租约或网盘；
- 新机不显示 `0 keep`，已有节点显示且可零线路修改；
- 所有必选项拒绝空输入；
- `APPLY` 前无上传，失败后无交接/清理/开面板；
- 端口组、一次性输入、内部修订升级、降级拒绝、卸载和拆除语义均通过自动或静态测试。
