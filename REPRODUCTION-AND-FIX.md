# v0.6 问题复现与定位

## v0.6.8 连接缓存与长期 key 耦合

旧实现的 `getConn()` 在进程内存在连接时直接返回缓存，并且菜单 `[1]` 无条件调用 `ensureKey()`。结果是登录方式没有明确选择，使用者也很难直观看出 `[1]` 仍在复用上一台 VPS。

v0.8.2 将所有远端操作收敛到统一的 `getActionConnection()` / `runRemoteAction()` 生命周期：

1. 每个远端菜单项都先显示临时密码/长期 key/取消二级菜单；
2. 每项重新输入目标 VPS、用户和端口，不复用维护连接缓存；
3. 临时模式使用系统临时目录中的一次性 key 和 `known_hosts`；
4. 图形版使用遮罩密码框，并通过当前用户专用随机命名管道把密码交给 Windows OpenSSH；CLI 兼容模式仍可直接继承终端；
5. 主菜单统一操作包装使用 `defer`，保证所有正常成功/失败返回都进入撤销分支；
6. 远端按完整公钥逐行删除，本机临时目录通过前缀和 Temp 根目录双重边界检查后删除；
7. 临时连接返回主菜单后从 App 状态移除；下一项操作即使存在长期 key 也重新选择目标；
8. 密码验证成功后才询问是否绑定，拒绝则清理，接受才显示长期私钥；
9. `[K]` 的解绑先远端精确撤销，确认成功后再把本机 key 目录移入可恢复备份。

回归测试覆盖所有远端菜单统一包装、明确二级选择、缓存 VPS 不得复用、临时目录边界、未绑定私钥不得显示/复制、远端只删除目标公钥、绑定文件禁止覆盖、解绑目录可恢复移动、SCP Host Key 固定，以及直接前代版本仍在升级/卸载清单。

## v0.6.7 Host Key 扫描误判

用户现场表现为 SSH TCP 可达，但首次连接立即报告“无法取得 VPS SSH Host 公钥”。对同一公开 SSH 端点执行只读扫描时能返回 RSA、ECDSA 和 Ed25519，因此不能把“单次扫描进程非零/瞬时无输出”等同于“服务器没有 Host Key”。

v0.8.2 的修复为：

1. 解析并固定同一目录中的完整 Windows OpenSSH 套件；
2. 最多三次有界扫描，包含显式算法尝试；
3. 只接受目标主机/端口、受支持算法、可解码且长度合理的 Host Key；
4. 有有效 key 时不因进程退出码非零丢弃 stdout；
5. 全部失败时输出所用程序绝对路径和逐次诊断，仍然失败关闭；
6. Host Key 得到用户明确确认前不生成登录私钥、不尝试密码登录。

回归测试同时覆盖：非零退出码 + 有效 key 必须接受，错误主机/端口和噪声必须拒绝，以及使用环境变量启用的公开 Host Key 集成测试。测试主机只在运行时通过环境变量提供，不写入源码或发布物。

## 可复现的最小条件

1. 远端自适应脚本已运行到 `DNS_OK`。
2. 随后的 Cover / Nginx / Certbot 任一步返回非零。
3. Windows 端 SSH 捕获把 stderr 与 stdout 合并或把错误文本当作返回值。
4. Windows 端无条件继续执行“读取交接单”和“打开 panel”的后续分支。

此时 OpenSSH 为带 PTY 的连接输出正常关闭提示：

```text
Connection to <host> closed.
```

旧分支没有业务标记/字段校验，因此这行 stderr 被包装进 `CREDENTIAL HANDOFF`。同时 v0.6 直到 Cover、证书、WARP 之后才创建 `/etc/proxy-runbook/public.env`，所以同一次失败又令 `PANEL_PORT` 为空。

## 修复

v0.6.1 同时修四层，避免只遮住表象：

1. 子进程层：stdout、stderr、退出码分别保存。
2. 协议层：交接单/面板元数据使用成对标记，并验证字段。
3. 控制流层：远端非零立即停止全部成功后动作。
4. 远端状态层：panel 元数据提前写入，失败阶段持久化，多来源回退读取。

## 回归测试

`parsers_test.go` 覆盖：

- `stdout=有效交接单` 且 `stderr=Connection closed.` 时，关闭提示不能进入交接单。
- 只有 `Connection closed.`、空标记块、无有效字段时必须拒绝复制。
- 空/越界 panel 端口必须拒绝。
- 远端退出码非零时，失败分支不得继续交接单或 panel 动作。
- 子进程非零退出码与两个输出流必须完整保留。

