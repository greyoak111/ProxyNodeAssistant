# ProxyNodeAssistant v1.0.0 从零部署教程

本教程从一台可 SSH 登录的 Ubuntu/Debian VPS 开始，直到真实客户端通过 Reality、Cloudflare/XHTTP 或双路访问互联网。

本版没有本机 admin/恢复门禁、UI 门禁、设备身份/准入、controller、邀请、租约或网盘。第一次使用不需要“批准这台设备”；只需要选择 VPS、SSH 用户、端口和临时密码/长期 key。

## 0. 先决定要哪种线路

| 方案 | DNS 准备 | 客户端线路 | 优点 | 代价 |
|---:|---|---|---|---|
| `[1]` 仅灰云 | 1 个 DNS only 主机名 | Reality `443` | 链路短、配置少、通常性能最好 | 源站 IPv4 可从 DNS 看到，直连路径可能受网络影响 |
| `[2]` 仅橙云 | 1 个 Proxied 主机名 | TLS + XHTTP `8443` | 隐藏源站，可绕开部分直连故障 | 依赖 Cloudflare，证书和边缘规则更复杂 |
| `[3]` 双路 | 1 个灰云 + 1 个不同橙云主机名 | Reality `443` + XHTTP `8443` | 两条路可手动切换 | 维护项最多，两条都要验货 |

已有节点还会显示 `[0] 保持现有线路`。新机不会显示 `[0]`，防止空节点被误判完成。

下面把你决定的模式、主机名和证书邮箱先写在自己的密码管理器临时笔记中。不要把真实值写进共享安装包、教程截图或公开 issue。

## 1. 准备 VPS

### 1.1 购买/重装要求

- KVM VPS；
- 独立公网 IPv4，不是只给少量映射端口的 NAT VPS；
- Ubuntu 22.04/24.04 或 Debian 12 一类受支持的 64 位系统；
- root，或能正常 sudo 的普通用户；
- 厂商控制台可以重装、重置密码并打开 Console/VNC；
- 至少 1 vCPU、1 GB 内存、10 GB 磁盘；2 GB 内存更宽裕。

### 1.2 先在厂商控制台记下

- VPS 公网 IPv4；
- SSH 用户；
- SSH 端口；
- 当前登录密码或已安装的 key；
- Console/VNC/救援入口位置。

这些信息只保存在你自己的密码管理器。ProxyNodeAssistant 的发行包不会内置它们。

### 1.3 本机先验证 SSH

Windows PowerShell 可做只读测试：

```powershell
ssh -p <SSH端口> <SSH用户>@<VPS公网IPv4>
```

首次连接先核对 Host Key 指纹。能登录后输入 `exit` 退出。若连 SSH 都失败，先在厂商控制台修正防火墙、sshd、账号或密码，不要进入节点施工。

## 2. 准备域名与 DNS

把域名接入 Cloudflare 后，确认区域状态正常。以下使用占位名说明；不要照抄占位值。

### 2.1 仅灰云

创建记录：

| 字段 | 填写 |
|---|---|
| Type | A |
| Name | 自选灰云子域名 |
| Content | VPS 公网 IPv4 |
| Proxy status | DNS only（灰云） |
| TTL | Auto |

灰云域名必须从公共 DNS 直接解析到 VPS。公网 80 需要能完成 ACME HTTP-01；正式 Reality 使用 443。

### 2.2 仅橙云

创建记录：

| 字段 | 填写 |
|---|---|
| Type | A |
| Name | 自选橙云子域名 |
| Content | VPS 公网 IPv4 |
| Proxy status | Proxied（橙云） |
| TTL | Auto |

然后在 Cloudflare 中确认：

1. SSL/TLS 为 **Full (strict)**；
2. Universal SSL 可用；
3. 该主机名没有 Cloudflare Access、Turnstile、质询、Worker、重定向或会改写 XHTTP 的规则；
4. 缓存规则对该主机名执行 Bypass；
5. 不创建“所有请求都把源站端口改到 8443”的 Origin Rule。客户端本身就使用 `https://橙云主机名:8443`，Cloudflare 会把该受支持端口转到源站同端口；HTTP-01 仍需让公网 80 到源站 80。

若已有旧规则，只在你明确知道其匹配条件时保留。尤其不要让 HTTP 的 `/.well-known/acme-challenge/` 被改写到 8443。

### 2.3 双路

创建两条不同记录：

| 用途 | 主机名 | Proxy status | 指向 |
|---|---|---|---|
| Reality | 灰云子域名 | DNS only | 同一 VPS |
| XHTTP | 橙云子域名 | Proxied | 同一 VPS |

两个主机名不能相同。菜单 `[1]` 会分别询问两套证书邮箱，即使你选择同一个邮箱，也必须在两个字段里亲自输入。

### 2.4 排除本地 DNS 假象

若 v2rayN TUN、全局 VPN、加速器、路由器安全 DNS 或浏览器安全 DNS 正在接管解析，检测结果可能与公网不同。施工前：

1. 暂停会使用目标 VPS 的全局 VPN/TUN；
2. 清理本机 DNS 缓存；
3. 用至少两个公共解析器核对；
4. 橙云域名应返回 Cloudflare 地址而不是源站 IPv4；
5. 灰云域名应返回源站 IPv4。

不要只因某次本地超时就删除正确的 DNS 记录。

## 3. 安装客户端

### 3.1 Windows

选择与系统架构匹配的正式文件：

```text
ProxyNodeAssistant-v1.0.0-win64.exe
ProxyNodeAssistant-v1.0.0-win32.exe
ProxyNodeAssistant-v1.0.0-win-arm64.exe
```

启动后程序会检查 OpenSSH。若系统没有，它只执行一次安装尝试和一次完整验证；失败后会告诉你缺哪个组件，不会不断重启安装。

### 3.2 Android

安装正式 universal APK。它不依赖 Termux。Android 上如果有全局 VPN 正使用目标 VPS，先关闭再施工，避免远端重启 Xray/WARP 后把当前 SSH 自己切断。

正式 Windows 与 Android 构建都内嵌远端工具包，并在施工时报告其内部 build ID；不要混用来源不明或 build ID 不一致的客户端包。

## 4. 第一次 SSH 选择

打开菜单 `[1]` 后，每次都重新选择：

### 临时密码

适合第一次连接。输入 VPS、SSH 用户和端口，核对 Host Key，然后在遮罩框输入密码。密码只用于认证；不会写入历史或普通日志。

### 节点长期 key

若当前设备还没有这台 VPS 的 key，程序先要求密码完成真实登录，再询问是否绑定。选择绑定后：

1. 本地生成独立 key；
2. 只上传公钥；
3. 用第二条连接验证 key；
4. 验证成功后才保存为该节点的长期登录方式。

长期 key 只是该 VPS 的普通登录凭据；不需要另一台设备批准。

## 5. 菜单 `[1]` 的逐项填写

程序先做只读检测。之后所有关键选择都必须有内容，不能靠空回车跳过。

### 5.1 选择线路

```text
0 = Keep / 保持现有线路（仅已有节点）
1 = Gray / 仅灰云直连
2 = Orange / 仅橙云 CDN
3 = Dual / 双路
```

新机从 `1—3` 中选一个。已有节点如果只是更新工具包、保持当前线路，才使用 `0`。

### 5.2 输入路线身份

- 灰云或双路：输入灰云域名、灰云证书邮箱；
- 橙云或双路：输入橙云域名、橙云证书邮箱；
- 双路必须是两个不同域名；
- 程序会校验格式，但不会替你猜域名或邮箱。

这些值只用于当前运行；安全偏好文件不保存它们。

### 5.3 选择伪装模板

程序会列出 15 套模板：

- 已有节点可输入 `0` 保持；
- `R` 真随机，并尽量避开当前模板；
- `A` 按域名稳定选择；
- `1—15` 指定编号。

如果你希望多台节点外观不同，用 `R` 或不同编号；希望同域名维护后仍保持一致，用 `A`。

### 5.4 选择性能

```text
0 = 保留（仅已有节点）
1 = 自动
2 = 低配
3 = 标准
4 = 高配
```

不确定时选择 `1 自动`。1 GB 小机优先自动或低配；不要因“高配”字样就强行选高配。

### 5.5 选择 WARP

```text
0 = 保持当前状态
1 = 确保开启
```

只想先跑通基础线路时可选保持。需要本工具管理的 WARP 本地代理和路由时选确保开启。

平台差异：当前 Android 对全新节点只接受 `1`，已有节点才允许 `0`；Windows 会显示 `0/1` 两项并要求明确选择。始终以当前界面的有效选项为准。

### 5.6 选择收尾

- 成功后是否整理多余备份；
- 成功后是否通过 `127.0.0.1` SSH 隧道打开 3x-ui。

已有节点无论如何先做变更前备份；这个保护不能关闭。

## 6. 核对施工预览

程序会显示：

- route mode；
- 灰云/橙云域名；
- 遮罩后的证书邮箱；
- cover choice；
- performance；
- WARP；
- 备份和收尾选择；
- 固定端口组 `443 / 24443 / 8443 / 40000`。

逐行核对。任何一项不对，输入其他内容取消。只有全部正确时精确输入：

```text
APPLY
```

在 `APPLY` 之前，程序不上传工具包，也不开始远端修改。

确认后，域名和邮箱通过随机文件名、仅 root 可读、权限 `0600` 的单次输入文件传给远端；核心/CDN 阶段分别使用并在成功、失败或回滚路径清理。它不是长期配置文件，不要手工复制或公开。

## 7. 施工中的人工验货

### 7.1 灰云 Reality

若需要新建或安全替换 Reality：

1. 程序在 `24443` 创建 shadow；
2. 打印临时 VLESS/Reality 链接；
3. 导入 v2rayN 或兼容客户端；
4. 选中临时节点；
5. 实际打开多个 HTTPS 网站，不只看 TCP 延迟；
6. 确认真能浏览后，才同意提升到 `443`。

“测试延迟有数字”只证明部分链路可达，不能证明代理流量可用。真连接失败就拒绝提升，保留原线路。

### 7.2 橙云 XHTTP

程序会先暂存 CDN 路线，再执行多层验证：

- 橙云域名没有直接解析到源站；
- `https://橙云域名:8443/` 返回 Cloudflare 的 `Cf-Ray`；
- 响应证明来自本工具受管的源站 `8443`；
- 从普通公网不能直接拨通源站 `8443`；
- 分享链接为橙云域名、端口 `8443`、TLS、XHTTP、有效 path/host/SNI。

之后：

1. 把打印的 `8443` XHTTP 链接导入客户端；
2. 实际浏览；
3. 确认后按提示精确输入大写 `REAL BROWSE OK`；
4. 程序才最终提交橙云或双路拓扑。

任何一步失败或未输入确认词，程序会回滚暂存变更。

### 7.3 双路

灰云和橙云都要通过各自验证。成功后客户端应看到两个独立节点：一个 Reality `443`，一个橙云 TLS/XHTTP `8443`。不要把二者的域名或端口混在同一条链接里。

### 7.4 菜单 `[5]` / `[6]` 凭据维护

安装完成后可单独维护登录凭据，不必重新跑菜单 `[1]`：

- `[5]` VPS 登录密码：`1` 随机生成、`2` 自定义、`0` 取消；
- `[6]` 3x-ui 账号/密码：`1` 随机生成、`2` 自定义、`0` 取消。

自定义密码在遮罩框中输入并二次确认；密码为 8—256 个字符且不能含换行或 NUL，面板账号还必须符合字母/数字/下划线/点/连字符规则。确认后秘密只经本次 SSH 的随机命名、root-only `0600` 一次性文件传递，远端脚本和客户端会在成功、失败、取消或中断时删除，不写入设置、命令行或普通日志。只有实际更新并通过完整交接单校验后才显示/复制真实值；取消不会修改 VPS。

## 8. 保存交接单

只有完整成功标记和有效字段通过校验后，程序才显示交接单。立即保存到密码管理器，重点包括：

- VPS 主机、SSH 用户和端口；
- 当前 SSH key 文件位置或管理方式；
- 3x-ui 用户名、真实密码、面板端口和 WebBasePath；
- 当前有效的 Reality/CDN 订阅信息；
- SS2022 TCP 订阅/访问链接（如已启用）；
- 备份位置和日期。

若旧面板只留下哈希，程序不能还原明文；需要明文就显式运行 `[6]` 轮换。保存后使用 `[12]` 清空剪贴板。

## 9. 上线后检查

1. 运行 `[3] 自动体检与排障`；
2. 运行 `[2]`，确认面板只能通过本机 `127.0.0.1` 隧道打开；
3. 从外部网络再测试正式订阅；
4. 双路分别测试 Reality 与 XHTTP；
5. 保存一份 `[9]` 完整灾备，或确认 `[15]` 的当前配置备份；
6. 确认服务商 Console/VNC 仍可用；
7. 不公开交接单、报告原件、私钥或备份。

## 10. 已有 v0.9.0 节点升级

不要先拆节点，也不要先删除旧工具包。

1. 使用重置版 v1.0.0；
2. 进入 `[1]`；
3. 选择当前可用的密码或长期 key；
4. 程序识别旧版本并声明升级；
5. 若线路保持不变，选 `[0] Keep`；若要切路，选 `1—3` 并重新输入需要的域名/邮箱；
6. 明确选择模板、性能、WARP；
7. 核对预览，输入 `APPLY`；
8. 等备份、升级和验收完成。

升级不是重装系统，也不应重复创建已有同构建节点。

## 11. 旧实验版 v1.0.0 重置

旧实验版和本版都显示 v1.0.0，所以不能只比较可见版本。重置版内部 revision 更高，菜单 `[1]` 会执行受控替换：

- 退役设备身份/准入、controller/邀请/租约和本机 admin/UI 门禁；
- 退役网盘/Copyparty 及其远端程序和入口；
- 保留 x-ui/Xray、Nginx、证书、线路、SSH、凭据和备份；
- 最后按新计划收敛。

重置版不会继续提供旧网盘或本机 admin 访问；遗留文件不会被迁移到活动配置。

## 12. 模式互切

已有节点切换灰/橙/双路仍使用唯一入口 `[1]`，不需要“拆链路重装”。

| 当前 → 目标 | 做法 |
|---|---|
| 任意 → Keep | `[1]` 选 `0`，只维护、不切路 |
| Orange/Dual → Gray | 准备有效灰云域名，选 `1`；事务完成后移除本工具管理的 CDN/XHTTP |
| Gray/Dual → Orange | 准备橙云设置，选 `2`；先暂存和验货，提交后移除本工具管理的 Reality |
| Gray/Orange → Dual | 准备两个不同主机名，选 `3`；旧可用线路保留到新线路验货成功 |

切换前强制备份；橙云事务失败时恢复切换前状态。

## 13. 卸载与恢复基线

### 只卸载管理工具：`[13]`

保留节点运行、配置、证书、凭据和备份。以后只能用 `[1]` 把管理工具装回来。

### 全量拆除：`[18]`

先生成完整救援包、下载到本机并核验 SHA-256，之后才拆除受管节点栈。SSH 配置、当前登录 key、22 端口和共享系统基础包保留。救援包下载/校验失败时不拆。

不要把 `[13]` 当作“恢复原状”，也不要把 `[18]` 当作普通更新。

## 14. 常见问题

### DNS 明明正确，程序却一直等

先关掉接管 DNS 的 TUN/VPN，换公共解析器核对。橙云应返回 Cloudflare 地址，灰云应返回源站。不要急着删记录。

### 为什么端口固定，不能随便填

Reality、Nginx、Xray、UFW、订阅、诊断、回滚和拆除必须同时理解同一端口。v1.0.0 只开放完整联调的端口组，避免“面板显示一个端口，订阅却导出另一个”的分裂状态。

### 为什么橙云入站内部端口不是 8443

3x-ui 内部 XHTTP 入站可以监听本地随机端口；客户端看到的应该是 Cloudflare 边缘 `8443`。Nginx/受限源站负责把边缘请求转到内部入站，二者不是同一层。

### 为什么 Android APK 比 Windows EXE 小

Windows 单 EXE 包含自包含 .NET/WPF 运行组件；Android 依赖系统运行环境。正式 Android 包仍必须内嵌同一远端工具包，大小差异不是缺功能的证据。

### 失败后能不能直接再点 `[1]`

先看失败阶段。若提示事务已回滚，可运行 `[3]` 核对后重试；若提示同版本工具包文件不完整，回到 `[1]`，检查预览并输入大写 `APPLY`，程序会原位修复管理工具后继续；若提示远端修订更高或构建 ID 不同，则换用匹配版本的客户端。不要在状态未知时反复重装。

## 15. 最终验收

- [ ] 没有本机 admin/恢复、UI 门禁、远端设备身份/准入、controller、邀请、租约或网盘；
- [ ] 线路、模板、性能、WARP 均为本人明确选择；
- [ ] 域名和邮箱为本人手填；
- [ ] `APPLY` 前没有上传；
- [ ] 端口组为 `443 / 24443 / 8443 / 40000`；
- [ ] 已有节点变更前备份成功；
- [ ] 真机验货不是只看延迟，而是真实浏览；
- [ ] 橙云源站 `8443` 无法被普通公网直拨；
- [ ] 失败没有连锁执行交接、清理或开面板；
- [ ] 交接单、私钥、API 密钥和灾备未公开；
- [ ] 菜单 `[3]` 与服务商 Console 救援路径均已验证。
